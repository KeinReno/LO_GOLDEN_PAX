# STATUS — единственная точка синхронизации агентов

Правило: **live ≤ ~120 строк**. Старые блоки → `agent-tasks/_archive/status/`.

---

## Live

### 2026-08-17 — modularization batch: diploOffers.mjs (FILE_DECOMPOSITION_SPEC batch 5) + второй баг найден (не чинил)
- **Статус:** landed, без коммита. Барель отдаёт ровно те же 10 имён (missing=[], extra=[]). Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/diploOffers.mjs` 812 → **20** lines. Вынесено в `server/diploOffers/`: `helpers.mjs` (314 — стор офферов, нормализация/валидация/трансфер айтемов сделки, запись отношений), `createOffer.mjs` (100 — `createDiploOffer`, эскроу), `unilateralStance.mjs` (137 — `applyUnilateralStance`: война/эмбарго/разрыв), `respond.mjs` (295 — `respondDiploOffer`, самая большая функция, accept/reject + перевод активов). 3 потребителя (`api.mjs`, `routes/diplo.mjs`, `turn/economyIntents.mjs`) не трогал.
- **Найден баг №2 (НЕ чинил, только флаг):** `respondDiploOffer` (теперь `server/diploOffers/respond.mjs`) — в ветке `research_pact` (было :674-675) вызывает `recomputeUnlocksFromTechs(..., world)`, но `world` объявлен через `const world = readLiveBoard()` **позже в той же функции** (было :686). Это классический JS TDZ (temporal dead zone) — `ReferenceError: Cannot access 'world' before initialization` на **каждом** принятии сделки с договором `research_pact`. Похоже, имелось в виду `worldPre` (уже прочитан парой строк выше для валидации активов) либо блок research_pact должен идти после чтения `world`. Тестов на `respondDiploOffer`/`research_pact` нет — та же картина, что и с `standDown` и `applyEspionage` сегодня: непокрытый тестами путь с обращением к необъявленной переменной. Сохранил как есть при переносе, оставил явный комментарий в коде на месте бага (не сама правка).
- **Файлы:** `server/diploOffers.mjs`, `server/diploOffers/helpers.mjs`, `server/diploOffers/createOffer.mjs`, `server/diploOffers/unilateralStance.mjs`, `server/diploOffers/respond.mjs`.
- **Дальше:** продолжаю по списку спеки. Похоже, стоит предложить владельцу отдельный проход "grep на использование переменных до объявления" по всему серверу — три однотипных бага за один день (`standDown`, `applyEspionage`, теперь этот) намекают, что это не единичный случай.

### 2026-08-17 — modularization batch: techActions.mjs (FILE_DECOMPOSITION_SPEC batch 4)
- **Статус:** landed, без коммита. Барель отдаёт ровно те же 23 имени (сверено кодом: missing=[], extra=[]). Full server suite **320/320**, `test:tech-offers` 17/17, `test:tech-progress` 14/14. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/techActions.mjs` 986 → **36** lines. Вынесено в `server/techActions/`: `helpers.mjs` (416 — резолв тех/апгрейдов, гейты свойств/рас/трейтов/линейджей, модификатор-стек, unlock-эффекты, canBuildWithTech), `queue.mjs` (303 — очередь исследований: canQueueTech/techAvailability/setResearchQueue/accelerateResearch/researchPathTo), `actions.mjs` (299 — мутирующие действия: researchTech/rerollResearchOffer/upgradeResearchedTechGrade/fillResearchedTechSocket/gmGrantTech/researchUpgrade). Зависимость односторонняя: actions → queue → helpers, циклов нет. 14 потребителей (включая `planetActions/`, `orderEngine.mjs`, `routes/economyTech.mjs` через динамический import) не трогал.
- **Файлы:** `server/techActions.mjs`, `server/techActions/helpers.mjs`, `server/techActions/queue.mjs`, `server/techActions/actions.mjs`.
- **Дальше:** продолжаю по списку спеки — дальше некритичные файлы среднего размера (diploOffers/ledger/intel/questEngine/gmCockpit/orderEngine/courtGovernance), затем correctness-critical (engagements/economyTick/combatResolve/normalizeWorld/stabilityRevolt) с повышенной осторожностью, cardBattle.mjs последним.

### 2026-08-17 — modularization batch: planetActions.mjs (FILE_DECOMPOSITION_SPEC batch 3) + bug found (not fixed, flagging only)
- **Статус:** landed, без коммита. Модуль грузится, экспортирует ровно те же 9 имён (`BUILD_QUEUE_MAX`, `listBuildingVariantsForPlanet`, `pushSystemHistory`, `canPlaceBuilding`, `applyPlanetAction`, `planetCapFromBuildings`, `collectPlanetYields`, `previewBuild`, `setBuildQueue`). Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/planetActions.mjs` 1161 → **20** lines (тонкий барель). Вынесено в `server/planetActions/`: `helpers.mjs` (342 — валидаторы владения/колонизации, разрешение зданий/колоний, AP/afford гейты, стоимость), `applyActions.mjs` (622 — сама `applyPlanetAction`, все 9 веток action: build/demolish/colonize/set_colony_type/rename/fill_slot/staff/staff_transfer/upgrade_grade, как единая функция — не резал на под-обработчики, там общие замыкания persistBoard/gateAp/recordIntent, риск не окупает выигрыш в строках), `queries.mjs` (226 — planetCapFromBuildings/collectPlanetYields/previewBuild/setBuildQueue). 6 потребителей (`api.mjs`, `economyTick.mjs`, `orderEngine.mjs`, `planetGrade.test.mjs`, `routes/economyMisc.mjs`, `routes/playActions.mjs`) не трогал — путь `./planetActions.mjs` тот же.
- **Найден баг (НЕ чинил, по правилу спеки — только флаг):** `previewBuild` (было :1102, теперь `server/planetActions/queries.mjs`) — `const cost = scaledCost(def.cost, buildCostMult(factionId, planet));` ссылается на переменную `planet`, которая **нигде не объявлена** в этой функции (есть только `found.planet`, но `{ system, planet } = found` не деструктурировано). Это `ReferenceError` при каждом успешном вызове — маршрут `POST /api/economy/preview-build` (`routes/economyMisc.mjs:134`) сейчас падает 500 на любой корректный запрос (buildingId существует, планета найдена). Теста на `previewBuild` нет вообще, поэтому баг не ловится прогоном. Сохранил баг как есть при переносе (не мой участок работы — правки багов идёт отдельным потоком в этом же `agent-tasks/`).
- **Файлы:** `server/planetActions.mjs`, `server/planetActions/helpers.mjs`, `server/planetActions/applyActions.mjs`, `server/planetActions/queries.mjs`.
- **Дальше:** продолжаю по списку спеки.

### 2026-08-17 — tech_paths validator gap + MEMORY.md correction (found by parallel audit agent)
- **Статус:** fixed, без коммита. `validate:tech` **0 errors, 8 warnings** (was 0/0 — the 8 are real, previously-invisible gaps, not new breakage). `validate:recipes` unaffected (4/4 same warnings as before).
- **Найдено (background audit agent, Alchemy+TechCatalog track):** 6 of 8 RoleScore path breakthroughs (`tech.path.{structural,energy,mobility,cognitive,biological,exotic}_breakthrough`) don't exist in `technologies.json` — a player who grinds RoleScore to 5000 sees a free-looking "Прорыв" button that always fails server-side (`"Неизвестная технология"`). Root cause: this content was authored twice in 2026-08-16 sessions, both landed "in working tree, no commit," both lost before ever being committed. `validate:tech` never cross-checked `tech_paths.json` against `technologies.json`, so this shipped with a clean 0-error validation run.
- **Сделано:** added a `tech_paths.json` cross-check to `scripts/validateTechnologies.mjs` (WARN-level, not error — the content file's own `meta.note` says clusters intentionally stay empty until a real authoring pass, so this is expected content debt, not a hard failure — but it must stay visible). Corrected `agent-tasks/MEMORY.md`'s false "biological filled 2026-08-16" claim and its overstated `canRaiseUnit` "building gate only" claim (real gap is narrower — see MEMORY.md).
- **Не сделано (намеренно):** did not author the missing 6 breakthroughs + ~24 cluster techs — that's content authoring, not a code fix; the existing `PATH_SIGNATURE_CATALOG` delegated workflow is the right tool for it, flagged in MEMORY.md with an explicit "commit immediately after validate:tech passes" warning to stop the loss pattern repeating. Did not touch the UI side (`ResearchPathsPanel.tsx`/`buildPathStripRows.ts` defaulting missing cost to 0, making the broken button look free) — that's a real, separate small UX fix, listed in the owner's problem-map, not done here.
- **Файлы:** `scripts/validateTechnologies.mjs`, `agent-tasks/MEMORY.md`.
- **Дальше:** re-run `PATH_SIGNATURE_CATALOG` for the 6 missing paths, committing each one once `validate:tech` shows it resolved. Separately: make the "Прорыв" button hide/disable (not show a free-looking 0 cost) when its breakthrough tech doesn't resolve, so this fails safe even if content gaps recur.

### 2026-08-17 — Correctness sweep round 2: applyEspionage crash (found by parallel audit agent)
- **Статус:** fixed, без коммита. Import fix verified (module loads clean, all 6 call-site signatures cross-checked against `ledger.mjs`/`opinionTick.mjs` exports). **No automated regression test added** — see Дальше, real risk to do carelessly.
- **Найдено (background audit agent, Intel/Fog+Diplomacy track):** `server/turn/intelIntents.mjs`'s `applyEspionage` called `ensureAllFactions`, `readLedger`, `ensureFactionEco`, `adjustStock`, `writeLedger` (all really from `ledger.mjs`) and `bumpOpinion` (from `opinionTick.mjs`) — **none imported**. Registered live in `appliers.mjs` (`"intent.espionage"`), invoked unguarded in `processTurn.mjs`'s intent loop. `processTurn.mjs` wraps the whole tick in try/catch and rolls back to the `pre_tick` snapshot on any throw — meaning **any player submitting an espionage intent would have silently rolled back that entire turn for every faction on the table**, not just their own action. Same bug class as `standDown` (helper called, never imported) but bigger blast radius.
- **Сделано:** added the missing import block. Verified module loads clean and every call site's args match the real export's signature exactly.
- **Why no test added**: `readLedger`/`writeLedger` hit `data/ledger.json` directly (`tableStore.mjs`'s `LEDGER_PATH` is hardcoded, no test/env override, no DI). A naive test calling the success path for real would mutate a real faction's live cognitio stock as a side effect of running the suite.
- **Файлы:** `server/turn/intelIntents.mjs`.
- **Дальше:** same audit agent found `intelIntents.mjs`/`diploOffers.mjs`/`opinionTick.mjs`/`fogStore.mjs` have **zero test coverage** ("exactly the file with the fatal bug has no test, presumably why it shipped"). Real fix: give `tableStore.mjs` a test-time data-dir override, then backfill tests. Own small workstream. Коммит по просьбе.

### 2026-08-17 — modularization batch: playerShare.mjs (FILE_DECOMPOSITION_SPEC batch 2)
- **Статус:** landed, без коммита. Модуль грузится, экспортирует ровно то же (`configureCloudPubToken`, `configureNgrokAuthtoken`, `getPlayerShareStatus`, `startPlayerShare`, `stopPlayerShare`). Выделенного теста на этот файл нет (ops-тулинг, не игровая логика) — проверено полным прогоном. Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/playerShare.mjs` 1260 → **336** lines (остался тонким оркестратором: определение целевого порта, публичный статус, старт/стоп). Вынесено в `server/playerShare/`: `base.mjs` (383 — общий стейт-синглтон + сетевые/процесс-хелперы), `cloudpub.mjs` (234 — провайдер CloudPub), `ngrok.mjs` (151 — провайдер ngrok), `otherTunnels.mjs` (84 — localtunnel + cloudflared), `watchdog.mjs` (173 — health-watchdog и soft-reconnect). Оба потребителя (`api.mjs`, `routes/playersShare.mjs`) как импортировали 5 функций из `./playerShare.mjs` / `../playerShare.mjs`, так и импортируют — путь не менялся.
- **Файлы:** `server/playerShare.mjs`, `server/playerShare/base.mjs`, `server/playerShare/cloudpub.mjs`, `server/playerShare/ngrok.mjs`, `server/playerShare/otherTunnels.mjs`, `server/playerShare/watchdog.mjs` (все новые, кроме первого).
- **Дальше:** продолжаю по списку спеки, файл за файлом с тестами после каждого.

### 2026-08-17 — modularization batch: narrative.mjs (FILE_DECOMPOSITION_SPEC batch 1)
- **Статус:** landed, без коммита. `quickcheck` 5/5. `test:court-roster` 11/11. Full server suite (`node --test server/*.test.mjs server/routes/*.test.mjs`) **320/320**. `tsc --noEmit` чисто.
- **Сделано:** `server/narrative.mjs` 1512 → **10** lines (thin re-export barrel, `export * from`). Two independent sub-responsibilities extracted with zero cross-imports between them: `server/narrative/poiTimersRefugees.mjs` (502 lines — POI/space-object tags, system timers, consequence presets, refugee spawn/convoy, depot attack range) and `server/narrative/courtTasks.mjs` (1015 lines — NPC court tasks, postings, council seating/portfolios, bloc/race leaders, court event ring buffer). All 9 existing consumers (`api.mjs` via `routes/narrative.mjs`, `courtRoster.mjs`, `economyTick.mjs`, `engagements.mjs`, `intents.mjs`, `logistics.mjs`, `processTurn.mjs`, `stability.mjs`, `courtConsumers.test.mjs`) kept importing from `./narrative.mjs` unchanged — barrel re-exports everything, no call sites touched. Behavior-preserving only, no logic changed.
- **Файлы:** `server/narrative.mjs`, `server/narrative/poiTimersRefugees.mjs` (new), `server/narrative/courtTasks.mjs` (new).
- **Дальше:** `courtTasks.mjs` at 1015 lines is still sizable but cohesive (all NPC-posting/council validation share the same shape) — left as one file per spec's "don't chase a number at the cost of a bad split" guidance; could revisit (postings vs. council-seating vs. tasks) in a later pass if it grows further. Next candidates from the spec's inventory: `engagements.mjs` (1391, correctness-critical — extra caution), `economyTick.mjs` (1318, correctness-critical), `playerShare.mjs` (1260), `combatResolve.mjs` (1211, correctness-critical). `cardBattle.mjs` (2932, largest, correctness-critical) deferred to last given its size and lack of a dedicated test file.

### 2026-08-17 — Correctness sweep (Claude, owner-directed) + FILE_DECOMPOSITION_SPEC handoff
- **Статус:** 2 real bugs found+fixed, без коммита. Full server suite (`node --test server/*.test.mjs server/routes/*.test.mjs`) **320/320**, full client TS suite **162/162**.
- **Сделано:**
  1. `stabilityRevolt.mjs` — `standDown(world, planet)` was called (:501, :544) but never defined → `ReferenceError` on any garrison-instant-suppress or stability-recovery tick. Defined it (clears revolt fields + `removeForce` + `pruneEmptyRebelFaction`, both already-existing-but-orphaned helpers — clearly meant to be called from here). Added 2 regression tests to `stabilityRevolt.test.mjs`; the recovery-path one genuinely reproduces the crash pre-fix.
  2. `spaceObjects.test.mjs` — "planet deposits stay infinite" test asserted on `.B[1].rate` for a `map.iron` fixture that's `category: "A"` (matches real `content/core/map_resources.json`) — test typo from today's labor-census batch, not a real regression. Fixed to `.A[1]`.
  - **Investigated, already resolved (no action needed):** the 2026-08-16-audit's Pollux/Aliot slot-overflow + 340-unowned-worlds census bug. Live-checked `data/published.json` (turn 66, current): Домус Алиот 167,223 pop / 39 surface buildings (cap 48), Мир Поллукс 263,953 / 36 (cap 38) — both within cap, both far below the old multi-million blowup. Zero planets galaxy-wide over their surface/orbital slot cap. Zero unowned planets with any population (was ~8.6e9 across 340 worlds). Today's "Census→labor" pass already closed this — the "клэмп Поллукс/Алиот" item in the entry below is stale, can be dropped from Дальше.
- **Файлы:** `server/stabilityRevolt.mjs`, `server/stabilityRevolt.test.mjs`, `server/spaceObjects.test.mjs`.
- **Дальше:** broader correctness sweep continuing (this was a first pass: fix what standard test runs surface, not yet a full static/undefined-reference audit — no linter configured in this repo, `npx eslint` needs an install the owner hasn't approved). `agent-tasks/FILE_DECOMPOSITION_SPEC.md` written and handed off separately (continuation of the api/processTurn/MapCanvas/ViewerPage split onto the remaining 18 server + 44 client files still over ~600 lines) — owner is low on Cursor limits, wants a separate Claude session to pick that up in parallel. Коммит по просьбе.

### 2026-08-17 — v0.6.0 на GitHub
- **Статус:** ветка `0_6`, тег `v0.6.0`, push на `origin`. `GMap/package.json` / Tauri / Cargo = **0.6.0**.
- **В коммите:** накопившийся стол 0.5→0.6 (труд/ценз, граф науки, civic paths, модули флот≠легион, viewer play session, двор/квесты, GM studios).
- **Не в git:** `art/` (~385MB постеры), live `table.sqlite*`, `tick-journal.json`, `.claude/settings.local.json`, секреты.
- **Дальше:** aliases депозитов; клэмп Поллукс/Алиот и потолок ambient; дипломатия/управление как civic-лестницы.

### 2026-08-17 — Сироты модулей + живые законы + гейт станций
- **Статус:** в теге `v0.6.0`. theater+laws **30/30**. `validate:tech` 608/0.
- **Цехи:** ион, сканер, лазер-карабин, ПТ, абляция, гусеница. Сканер → слот тактики (space).
- **Станции:** trade=`market_hall`, military=`kinetic`, science=`archive_hall`, relay=`chem_drive`; mining без гейта.
- **Законы:** `open_markets` +5% supply, `state_religion` +1 лояльность — эффекты из `civic_paths`, не хардкод id.

Архив: `_archive/status/2026-08-13.md` … `2026-08-17.md`.
