# STATUS — единственная точка синхронизации агентов

Правило: **live ≤ ~120 строк**. Старые блоки → `agent-tasks/_archive/status/`.

---

## Live

### 2026-08-19 — GM chrome pass 7
- **Статус:** в дереве, без коммита. Тик не гонял. Караван не ставил.
- **Инспектор:** «Квесты и караваны» → **На системе**. Кнопка «Доска квестов · F7». Караван только с confirm цели.
- **F7 Сессия:** вкладка **Доска** (все квесты стола), не третьи «Квесты».
- **Доказ:** live Гурез «На системе» + ярмарка; F7 вкладка Доска; консоль 0 error.

### 2026-08-19 — GM chrome pass 6
- **Статус:** в дереве, без коммита. Тик не гонял. Контент станций не писал.
- **Сооружения:** вкладки Здания / Станции / Космо-объекты. Jump из Каталогов сажает на нужную вкладку.
- **Каталоги:** станции и космо-объекты убраны из JSON-навигации.
- **Список:** станции без фейкового «тир/поверхность»; зона-фильтр только у зданий.
- **Доказ:** live 5 станций + 19 космо-объектов; JSON-дублей в нави нет.

### 2026-08-19 — GM chrome pass 5
- **Статус:** в дереве, без коммита. Тик не гонял.
- **Мастерская:** группы Конструкторы / Без конструктора. «Каталог техов» → **Технологии** (JSON-вкладка в студии).
- **Каталоги:** нет второго JSON техов/юнитов/зданий/рас; кнопки «открыть» ведут в студию. Остались квесты, рецепты, совет, станции.
- **Имена:** левый таб **Ведущий** ≠ док F7 **Сессия**.
- **Доказ:** live `127.0.0.1:5173/` jump → `app-shell--tech` + JSON tab; tsc по затронутым 0.

### 2026-08-19 — GM chrome pass 4
- **Статус:** в дереве, без коммита. Тик не гонял.
- **F5:** EditorHotkeys больше не preventDefault — браузерный refresh.
- **Карта без хрома:** `\` (Backslash) скрывает шапку/нотч/панели; чип «UI · \\».
- **Имена:** F7 Сессия; шапка Чат ≠ RP Стол; Мастерская → Каталоги (не Ателье).
- **Космос:** 8 частых; поиск «черв» → Червоточина; поиск не включает «все».
- **Доказ:** `tsc --noEmit` 0; live `127.0.0.1:5173/` F5 defaultPrevented=false, map-only top=none.

### 2026-08-19 — Аудит Сил — фикс
- **Статус:** P0+P1 в дереве, без коммита. Тик не гонял.
- **Док:** z 380 (вкл. v2), backdrop выше дока. Клик «Силы» поверх Штаба проходит.
- **Обложки:** имя, N кораблей, роли, HP-бар. Compact `.forces-covers-grid`.
- **Копия:** субтитр loadout; лом≠роспуск; Esc: оснащение → карта → колода → комната.
- **Доказ:** 8/8 `forceHp`+merge+outfit+subtitle; `/view` Белатор ход 18, SOL INVICTUS.

### 2026-08-19 — Дипломатия: фикс очереди аудита
- **Статус:** в дереве, без коммита. Тик не гонял, входящие не принимал.
- **Стол:** Accept hold на флот/мир/тех/союз; Reject клик; без «Списка»; война/биржа в досье; эскроу «заморожено»; fairness под ⇄.
- **Intel:** `opinionTowardViewer` на маске L1–3; сравнение «по разведке N/4».
- **Хром:** mobile 5 = дипло; F1–F9 capture в GM-шеллах; GM overlay под топбаром.
- **Доказ:** `diploDealCards.test.ts` 7/7; `intel/masking.test.mjs` 3/3.

### 2026-08-19 — Аудит данных: модули + расы
- **Статус:** в дереве, без коммита. Тик не гонял.
- **AUD-03:** `map_resources` = 86 руд, `modules` = 20; утечек `module.*` в рудах 0. Outfit берёт `outfitResourceBag`.
- **AUD-05/06:** +15 форков в `races.json` (validate:races 33 ok). `primaryRaceId` проставлен 25 державам (Белатор/Карнед/Корвун/ХэШах/Коринфад). 9 пустых — павшие/без состава.
- **Доказ:** theater+depositExtract+factions+resourceIndex+card-battle зелёные.
- **Не трогал:** науку, `processTurn`. AUD-09/11-unowned/32 ещё открыты.

### 2026-08-19 — Карта: стек + chrome UX
- **Статус:** список из обзора в дереве, без коммита. Тик не гонял.
- **Стек:** 1 глиф/держава ×N, веер по выбору; `[` `]` и повторный клик по своему глифу. Тесты 8/8.
- **Chrome:** GM topbar клееный (margin 0); рельс 2×2; RP z=520, close кликается; Attention без «Стол спокоен», чипы с именем системы; `/` фокус поиска.
- **Карта игрока:** режимы F4–F10 на вьюпорте; dropzone без пунктира, подсветка только при драге стока.
- **Доказ:** layout test 8/8; live GM railH=60, topY=0; `/view` drop `border: none`, 0 console error.
- **Не трогал:** дубль Attack в инспекторе.

### 2026-08-19 — Штаб: фикс аудита
- **Статус:** в дереве, без коммита. Тик не гонял. Live `/view` Белатор ход 18.
- **Внимание:** combat 1 строка → экономика → дипло → квесты/RP → очередь → idle. Адара энергия первая; «7 флотов».
- **Диплинк:** узкое место → Экономика/Производство + фильтр D + Адара, не Политики.
- **UI:** чипы честные (4 войны / есть что изучить / RP); scout/бой/notes убраны; briefing открыт; Путь не RoleScore.
- **Доказ:** `playerUiTerms`+`buildViewerAlerts` 5/5; tsc по HQ-файлам чист; 0 console error.

### 2026-08-19 — Экономика HUD/хоткеи (фикс)
- **Статус:** P0+P1 из аудита в дереве, без коммита. Тик не гонял. `/view` PIN не открывал.
- **Сделано:** вкладки Alt+1–5 (сырые 1–5 = док); полоса metal/supply/пег + клик A–F в производство / клик трат на склад; «К системе» = дефицит; нет фейкового `~rate`; обмен 5 металла не «5M»; ГМ казна без `module.*` и hidden налогов; мёртвый `.eco-*` CSS.
- **Доказ:** `node --experimental-strip-types --test src/viewer/economy/productionData.test.ts` 2/2. `tsc` по затронутым файлам чист (красные в CourtPanel/ForcesDeck/alerts.test — не этот дифф).
- **Не трогал:** P2 (RoleScore, законы, GameRulesStudio, Market rewrite).
- **Дальше:** коммит по просьбе. Живой `/view` с PIN — полоса и Alt+цифры.

### 2026-08-18 — RoleScore путь mobility
- **Статус:** в дереве, без коммита. Тик не применял.
- **Прорыв:** `tech.path.mobility_breakthrough` (70 знания). Кластер 4 живых: прыжок, блиц, развёртывание, червоточины.
- **Не трогал:** structural/energy (якоря), cognitive/biological/exotic.
- **Дальше:** коммит по просьбе. Следующий путь — cognitive.

### 2026-08-18 — Аудит W3/W4 (науку не трогал)
- **Статус:** в дереве, без коммита. `processTurn` не гонял.
- **Модули:** `/api/content` ключ `modules` (20); `byCategoryDeposits` без пушек. Склейку map_resources не снимал.
- **Тик:** extractor/yield вне биома skip. Стол: disabled 13 off-biome + 17 построек на pop=0. 321 `buildingId` = `building.<kind>`.
- **Карта:** 17 коридоров к изолированным своим stellar (0 осталось); 3 дубля имён; `star` в pois; `lithogenesis` в schema.
- **Доказ:** depositExtract+theater 39/39; eslint затронутых server 0.
- **Не трогал:** tech_paths/ResearchPanel, unmerge contentLoader, ничейные шахты пояса, расы.

### 2026-08-18 — лестница найма (линкор / тяжёлый легион)
- **Статус:** в дереве, без коммита. Тик не применял.
- **Флот:** линкор = `battleship_keel` (`tech.military.battleship_keel`). Рёбра крейсера больше не поднимают линкор. Дредноут теперь требует киль линкора, не фрегат.
- **Легион:** тяжёлый = `heavy_legion` (`tech.military.heavy_legion`). Линейная пехота не поднимает тяжёлых.
- **Дальше:** коммит по просьбе. Пути RoleScore не заполнял.

### 2026-08-18 — Игрок: стол дипломатии
- **Статус:** в дереве, без коммита. Тик не гонял.
- **Стол:** колонки «отдаю / прошу» = drop-зоны. Рука: казна (металл/supply/A–F + сырьё), силы/миры, договоры (вкл. миграция/вассал). Клик «На стол» или жест.
- **Центр:** война/эмбарго и биржа контакта свёрнуты. Карты показывают запас и где стоит флот.
- **Доказ:** `diploDealCards.test.ts` 3/3. `tsc` по затронутым файлам чист.
- **Дальше:** проверить в `/view` жест на живом столе.

### 2026-08-18 — Аудит W1/W2 (науку не трогал)
- **Статус:** в дереве, без коммита. Тик/`processTurn` не гонял.
- **Бои:** zombie card SYS-867 `eng_1785918557450_lo2a` → cancelled (ghost UUID / флот уехал). Таймаут карт + abandon в `engagements/abandon.mjs`. Теория-Док ещё active 2/3 — закроется на следующем тике.
- **Каталог:** `ship.transport`; алиасы корпусов (разведчик/рейдер/торговый/дрононосец…); `unit.assault`→breach; `armor_cadre` role armor; 5 интентов + produce AP из каталога. `транзит-пошлина`→trade_value; terrestrial→rocky; trade Карнед↔Амальфея двусторонний; 25 станций в своих системах получили factionId.
- **Доказ:** `abandon`+`properties` 5/5; `forceRecruit` 21/21; eslint changed server 0; live missShip/missUnit **0**.
- **Не трогал:** technologies/tech_paths/ResearchPanel/ledger unlockedTechs. Модули в map_resources, 6 прорывов, изолированный куст ОР, пустые шахты Белатора, ничейные шахты пояса.
- **Дальше:** W3 хвост (модули, биом-здания, empty pop) по просьбе.

### 2026-08-18 — GM: система / планета
- **Статус:** охота live 5173 + фикс UI. Тик не гонял. `/view` (PIN) не логинился. Черновик не сохранял.
- **Ок:** Солис → Домус Солис. Коридор «Западный Перевал» — 0 планет.
- **Починено:** дубль «Мир-ядро»; теги `map.iron`/`железо`; шапка снабжения; GM «+ Постройка» пишет `buildingId` из каталога (67 опций, дефолт `building.residential`).
- **Доказ:** `depositPaint.test.ts` 4/4; `gmCatalogBuildings.test.ts` 3/3. Live: новый ряд «Жилой район · T1» / `building.residential`, undo → 6/48.
- **Не ходил:** PlayerPlanetManage `/view`.
- **Дальше:** коммит по просьбе.

### 2026-08-18 — паспорт имён (коллектор / Чёрное Железо)
- **Статус:** в дереве, без коммита. Тик не применял.
- **Коллектор:** `requireProperties: anomaly_tap`. Буква A9 больше не открывает. Ключ — `tech.anomaly_tapping`.
- **Чёрное Железо:** киль `black_iron_keel` с `tech.black_iron_forges`. Дредноутный киль не поднимает именной флагман.
- **Дальше:** коммит по просьбе. Лестница найма не трогал.

### 2026-08-18 — Аудит игровых данных: документ + план
- **Канон:** `agent-tasks/GAME_DATA_AUDIT_2026-08-18.md` (AUD-01…33, волны W0–W6). Тик не гоняли; коммит не делал.
- **Частично закрыто другими:** skip `catalogPending` в тике + scrub; `logistics.depot` в каталоге; UI-фильтр модулей. Открыты: склейка `contentLoader`, zombie card-battle SYS-867.
- **Дальше:** W1 (бои + мусор ходов) → W2 (корабли/расы/интенты) → W3 экономика. Не параллелить merge модулей.

### 2026-08-18 — Login coverflow + GM карточки входа
- **Статус:** в дереве, без коммита. `/view`: 3D-карусель Silver Hearts · Golden Pax · Final Crusade; постер Pax; RPG/SAGA тот же набор.
- **GM:** Ателье → «Карточки входа» — описание и загрузка постера.
- **Доказ:** `gateCampaigns` 3/3; catalog 3/3; browser `/view` 0 error; `/api/gate-campaigns` 200.

### 2026-08-18 — Наука: orbit-редизайн вживлён в `ResearchPanel.tsx`, готов к живой проверке
- **Статус:** в рабочем дереве, без коммита. `tsc --noEmit` чисто по всему проекту, `npm run build` чисто (только пре-существующие warning про чанки/динамические импорты). `test:viewer-page` **125/125**, `src/viewer/research/orbit/*.test.ts` **23/23**.
- **Сделано в этом заходе:** `OrbitGraph.tsx` (SVG-рендер секторов/узлов на `computeOrbitLayout`+`computeDirectionProgress`+`computeHorizon`, pan/zoom, drag-to-queue и drop-Знания-на-узел сохранены), `SpecializationStrip.tsx` (тир 3 — гейджи RoleScore, сгруппированные по направлению через новый `directionRoleScorePaths.ts`), `computeDirectionProgress.ts`+`computeHorizon.ts` (чистая логика §1d/§1c, 17 тестов). Полностью переписан `scienceMode==="tree"` в `ResearchPanel.tsx`: убраны `research-branch-tabs`, `ResearchPathsPanel`-фолд, `TechGraphCanvas`; очередь+офферы подняты в always-visible `orbit-topbar`. Добавлен CSS-блок (`.orbit-*`, ~330 строк, свои классы — не `.research-*`, конец `app.css`) и 3-я колонка в `.research-workbench` через `:has(.orbit-spec-strip)`.
- **Резолвнута нестыковка в самой спеке** (§1e говорит про 6 силуэтов по направлениям, §1f — про "8 flagship-узлов" по RoleScore-путям): свёл через `tech_directions.json`'s `paths` — 8 материальных RoleScore-путей реально принадлежат направлениям (industry←4, military←3, governance←1, diplomacy/culture/commerce←0 по факту данных), civic-пути (торговля/культура) остаются в Бирже/Дворе, не в Науке.
- **Совпало с параллельным агентом:** он независимо докрутил тот же `SpecializationStrip.tsx` — добавил `state/techPathLive.ts` (`liveBreakthroughTechId`), чтобы "ready" не загоралось для путей без реально существующей (не `catalogPending`) прорыв-технологии — это чинит ещё один слой старой проблемы "6 из 8 путей сломаны". Оставил как есть, не откатывал.
- **Сознательно урезано в этом проходе (не забыто, не отдельная спека):** рёбра-пререквизиты не рисуются (только положение/состояние узлов — сама антиперегрузочная механика, не визуальная связность); мини-карты в развёрнутом виде нет; §1f drag-to-combine НЕ начат (по спеке и так последний, блокирован контентным гэпом); `TechGraphCanvas.tsx`/`ResearchPathsPanel.tsx` осиротели, но не удалены — рано, пока редизайн не проверен живьём.
- **НЕ проверено вживую в браузере** — только `tsc`/тесты/сборка. Дев-сервер не поднимал, кликов не делал — на столе сейчас активно работают другие агенты (Наука в т.ч.), не хотел плодить конфликт портов/сессий.
- **Дальше:** живая проверка в браузере (горизонт по умолчанию, разворот сектора по клику на бейдж, drag Знания на узел, силуэт-трофей на 100%-секторе если найдётся); затем §1f, затем удаление осиротевших старых файлов.

### 2026-08-19 — Наука: живьём всё ещё выглядело плохо (не баг, а сама модель "колеса") — геометрия переделана на созвездие независимых кластеров, владелец подтвердил направление
- **Триггер:** владелец прислал новый живой скрин после всех 5 фиксов ниже — размер/CSS больше не сломаны, но результат всё равно "неудобно, ничего не понятно": один большой цветной клин (industry), остальное — чёрная пустота внутри того же круга.
- **Диагноз (не CSS, не число):** `stageSize` в `OrbitGraph.tsx` — ОДИН общий радиус на весь холст, посчитанный по самому крупному сектору (industry, ~550px). Каждый сектор красит клин только до СВОЕГО `outerRadius` (у governance — 130px), а дальше и до общего края холста — просто пустой фон. Это системное свойство "общий радиус на всех при разном объёме контента", не лечится числами/CSS.
- **Владелец выбрал вариант "Созвездие"** из 3 предложенных (равномерный клин на всех / созвездие независимых кластеров / только крупные на колесе) — независимые кластеры без общего радиуса.
- **Новая модель `computeOrbitLayout.ts` (переписан):** каждое направление — свой "лепесток" с фиксированным равным углом (`clusterArcDeg`, 110°) и якорем на фиксированном расстоянии от хаба (`hubGapPx`, 70px) на своей компас-точке (6 направлений = 60° друг от друга). Кольца растут от СВОЕГО апекса, размер — только от своего контента. Удалена вся система `allocateSectorAngles` (пропорциональные доли + потолок/пол угла из вчерашнего фикса) — она была нужна только для старой "общий радиус" модели, в новой не нужна вообще (упрощение, не костыль).
- **Живые числа (355/76/123/60/0/1 техов):** governance/diplomacy теперь `globalReach` ~90px от хаба (компактная точка у хаба), military/commerce/culture — 290-380px, industry честно растёт до 576px, но это НЕ пустая трата — там реально упаковано 355 узлов на широкой (110°) дуге. `maxReach` был 550 (при живом баге до фикса — 2216), теперь 576 — сопоставимо по числу, но качественно другое: раньше это "1 клин + большая чёрная пустошь", теперь "6 лепестков-соцветий разного размера вокруг хаба, пустота только между ними" (ожидаемо и нормально для compass-раскладки).
- **`OrbitGraph.tsx`:** клин-подсветка, позиция трофей-силуэта, позиция бейджа "+N" — все пересчитаны от `s.apexX/apexY` направления, а не от глобального (0,0). Добавлена тонкая "спица" (`stem`) от хаба до апекса каждого направления — читается как компас/цветок, не как рандомные плавающие блобы.
- **Тесты:** `computeOrbitLayout.test.ts` переписан под новую модель (равный фиксированный угол на всех, независимость радиуса бедного направления от богатого соседа, апекс на равном расстоянии от хаба) — 8/8 зелёных. `tsc --noEmit` чист, `npm run build` чист (только пре-существующие warning).
- **НЕ проверено вживую в браузере повторно** — тот же блок по PIN-логину, что и раньше. Числа посчитаны напрямую по реальным `technologies.json`, не поверено на глаз.
- **Дальше:** дождаться следующего живого скрина от владельца — константы (`clusterArcDeg`/`hubGapPx`/`nodePitchPx`) подобраны расчётом на непересечение соседних лепестков, но не проверены глазами; если лепестки визуально наезжают друг на друга или выглядят слишком длинными/короткими — тюнинг just these 4 numbers, архитектуру трогать не придётся.

### 2026-08-18 — Наука: владелец увидел живой скрин ("маленькое, ничего не понятно"), полная перепроверка нашла 4 реальных бага — все починены
- **Триггер:** владелец прислал скрин живого рендера — крошечный шарик из точек на тонкой линии в пустом тёмном экране, попросил "перепроверь там всё на всякий случай" после того, как я до этого сдал заход выше только с `tsc`/тестами/сборкой (без живого просмотра).
- **Баг 1 (CSS, найден первым по скрину):** `.hq-card` (база `.research-panel`) без `display:flex`/явной высоты рвёт цепочку `flex:1 1 auto`/`height:100%` вниз до `.orbit-graph-stage` — `<svg width="100%" height="100%">` схлопывается в браузерный дефолт. Фикс: явные `min-height` на нескольких уровнях (640/520/460px), по образцу старого рабочего `.research-map .research-graph { min-height: 12rem }`. Заодно убрал дублирующийся `.orbit-svg {}` (был раздвоен на 2 блока).
- **Баг 2 (CSS, найден при перепроверке):** 3-колоночная grid-раскладка `.research-workbench:has(.orbit-spec-strip)` — ширины колонок были назначены не в том порядке, что реальный DOM (`research-map`, `orbit-spec-strip`, `research-detail`) — деталь-панель (нужна шире, там кнопки/апгрейды) получала более узкую колонку. Починено — ширины переставлены под реальный DOM-порядок.
- **Баг 3 (CSS, найден при перепроверке):** брейкпоинт 3-колоночного правила был `1180px`, а базового 2-колоночного — `880px` — зазор между ними ронял 3-й элемент в implicit grid row на средних ширинах. Выровнено на `880px`.
- **Баг 4 (React, найден при перепроверке):** силуэт-трофей (фаза "Завершено") в `OrbitGraph.tsx` масштабировался магическим числом (`p.y * 0.6 - 90`), не привязанным к реальной геометрии сектора. Починено — масштаб теперь считается от `s.outerRadius` сектора и реальной верхней точки (`tipY`) фигуры силуэта.
- **Баг 5 (логика, самый значимый, найден живым числовым прогоном против РЕАЛЬНЫХ 608 техов, не синтетики):** `computeOrbitLayout.ts`'s `allocateSectorAngles()` имел потолок угла сектора (135°, §3.2), но НЕ имел пола. `packRings()`'s формула радиуса — `(placeCount*nodePitchPx)/sectorAngleRad` — расходится при угле→0. На реальных данных (`industry` 355 техов, `governance` 1 тех) это дало `governance.outerRadius=2216` — **в 4 раза больше**, чем у `industry` (550) с 355 техами. Это раздувало общий `stageSize` до 4593 (было бы 1180 без бага) — сжимая ВЕСЬ реальный контент до ~11-12% от нужного масштаба. Правдоподобная главная причина "крошечного шарика" со скрина, а не только Баг 1.
  - **Фикс:** симметричный пол угла (`minSectorAngleDeg`, дефолт 15°) — та же итеративная схема перераспределения, что и у потолка, но **двумя последовательными фазами** (сперва потолок до сходимости, потом пол на остатке), а не одним смешанным проходом — смешанный проход флорит секторы преждевременно, судя по их доле от ещё-не-урезанного пула (поймано регрессией в уже существующем тесте "caps a lopsided sector..." — `b` ожидался 45°, получал 15°, пока не разнёс на фазы). Секторы с 0 реальных техов (`diplomacy`) исключены из пола — угол 0, нечего флорить.
  - **Провер на реальных данных после фикса:** `governance.outerRadius` 2216→**130**, `maxOuterRadius` 2216→**550** (теперь его держит настоящий крупный industry, как и должно быть), `stageSize` 4593→**1180** (в 3.9× компактнее).
  - **Добавлены 2 новых теста** в `computeOrbitLayout.test.ts` (пол реально режет радиус-блоуап на перекошенном реальном распределении; 0-tech секторы исключены из пола) — было 6/6, стало **8/8**.
- **Доказ после всех 5 фиксов:** `src/viewer/research/orbit/*.test.ts` — все subtests зелёные (0 `not ok` по всему пакету), `tsc --noEmit` чисто, `npm run build` чисто (только пре-существующие warning про chunk-size/dynamic-import).
- **Урок для себя, не только отчёт:** "tsc/тесты/сборка зелёные" НЕ ловит визуальные/layout-баги (CSS-каскад, магические числа масштаба, blow-up в чистой математике на реальных, а не тестовых данных) — только явный запрос владельца на переперепроверку вскрыл 4 из 5 багов. Живой числовой прогон против реальных данных (не только модульные тесты на синтетике) поймал самый крупный баг.
- **Всё ещё НЕ проверено вживую в браузере** — блокировано PIN-логином, credentials нет и не запрашивались/не угадывались.
- **Дальше:** живая браузерная проверка (см. выше); затем §1f drag-to-combine; затем удаление осиротевших `TechGraphCanvas.tsx`/`ResearchPathsPanel.tsx`.

### 2026-08-18 — пустые двери RoleScore / civic
- **Статус:** в дереве, без коммита. Тик не применял.
- **Пути:** 6 путей без прорыва — `breakthroughTechId: null`. Кластер без фейковых doctrine. Payload `ready` только если технология живая (не stub).
- **UI:** нет «Прорыв ещё не в каталоге»; кнопка Прорыв только у offensive/defensive.
- **Civic:** unlock без здания / без эффектов закона не в payload и не в тике. Живые законы не трогал.
- **Доказ:** `roleScores` + `civicPaths` + `techPathLive` + `validate:tech` + `tsc --noEmit`.
- **Дальше:** коммит по просьбе. Не заполнял 6 прорывов и catalogPending.

### 2026-08-18 — pending techs + logistics.depot
- **Статус:** в дереве, без коммита. Тик не применял. Леджер записан.
- **Тик:** `catalogPending` больше не даёт `production_mult` / unlock. `gmGrantTech` и grant t20 не выдают стабы.
- **Стол:** Белатор 130→34, Амальфея 113→21, Карнед 265→22. Тиры Белатора те же; Амальфея F 6→5; Карнед C/D/E/F вниз.
- **Депо:** `logistics.depot` в `buildings.json` (T1, `stability_add`, без labor). 35 инстансов резолвятся.
- **Доказ:** `techModifierEffects` + `techUnlockRebuild` 8/8; `lint:balance` ok (92 buildings); повторный scrub 0 dropped.
- **Дальше:** коммит по просьбе. Корпуса/расы на карте не трогал.

### 2026-08-18 — Player `/view` wave 2 (F + хвосты A–E)
- **Статус:** в дереве, без коммита. Тик/Save не гонял.
- **Логин:** `faction_nomad_*` / «· Галиван» не в `/api/factions`. Live: 19 держав, 0 Галиван.
- **ОД:** HQ/биржа/нырок/планета тоже `formatOdMeter`.
- **Наука:** нет tech → нет «Прорыв · 0»; «пилот» убран. Q/B/H по `e.code`.
- **Разведка HQ:** чужие, 20 ближайших + поиск. Дроверы `inert`. Чужие main/foreign квесты, sectors, loyaltyMatrix не в payload.
- **Доказ:** `tsc --noEmit` 0; `test:player-auth` 16/16; `pickScoutSystems` 2/2.
- **Не трогал:** master token в клиенте, `?p=` preview, 6 breakthroughs, combat tags.

### 2026-08-18 — Player gate: Legends of Oberon hub
- **Статус:** в дереве, без коммита. `/view` — хаб вместо dropdown-логина.
- **UI:** THE LEGENDS OF OBERON · STRATEGIA / RPG скоро / SAGA скоро (F1–F3) · карточка кампании · ключ 4+ цифр. RPG/SAGA — «скоро». Perf карты спрятан в «Параметры».
- **Auth:** PIN-only сажает в державу (factionId больше не обязателен на `/api/login`). Dropdown держав убран.
- **Доказ:** `playerAuth` 16/16; `test:viewer-auth` + catalog; browser `/view` 0 error/warn. Не логинился живым PIN в чат.
- **Дальше:** коммит по просьбе. Вторые кампании strategia не заводил.

### 2026-08-18 — GM walk: все студии + карта
- **Статус:** в дереве, без коммита. Тик не применял. Стол на месте: ход 17 · rev 630 · 978 систем.
- **Пройдено live:** Карта, RP, Симулятор, Наука (608), Юниты, Сооружения, Расы (18), Державы (Белатор открыт), Баланс, Ателье; F1/F2/F4/F9; Тик-превью (Отмена); Файл/Сессия. Консоль 0 error. `/api/*` 200.
- **Починено:** Державы → Карта больше не поднимает оверлей досье (`setGmShellMode` сбрасывает overlay state).
- **P2 осталось:** верстак F2 перекрывает F-док; частый poll `/api/intents`; в F9 лог catch-up `ensureAllFactions is not defined` (16 авг).
- **Дальше:** коммит по просьбе.

### 2026-08-18 — Наука ↔ постройки / найм (гейты)
- **Статус:** в дереве, без коммита. `tsc --noEmit` 0.
- **Меги:** архив Белатора F9, дамильские врата F8 (не с хода 1). Раса fail-closed на клиенте. `recomputeUnlocksFromTechs` заново берёт unlock с стоящих зданий (F10 архива не сгорает на пакте).
- **Найм:** именные корпуса/кадры получили свойства; сервер + UI смотрят фракцию и свойства.
- **UI:** станции в карточке науки; RoleScore на клиенте `canBuildWithTech`; реликтовое хранилище — `fillOnly` слот (не `anomaly` на постройку).
- **Доказ:** `server/techUnlockRebuild.test.mjs` + `test:force-raise` + `buildingAccess.test.ts`.
- **Дальше:** коммит по просьбе. Высокий tier без свойств (коллектор T10) не трогал.

### 2026-08-18 — GM chrome: студии vs карта, инспектор
- **Статус:** в дереве, без коммита. Консоль 0 error. `tsc` затронутых файлов чист.
- **Шапка:** иконки студий остаются. Вне карты спрятаны поиск/undo/держава/видимость/RP-float. Досье и Дипло убраны из бара → «Стол…» и студия «Державы» / F5. Сохранить/Билд/Тик — тот же язык кнопок.
- **Инспектор:** карточка выбранного объекта. Пустой стейт больше не зовёт досье/дипло. SystemEditor только в оверлее системы. Кнопка державы → студия «Державы». Оверлеи не перекрывают студии.
- **Левый rail:** вкладки как студии. «Карта» открыта сразу. Сессия без дублей шапки (билд/тик/RP/держава/кисти). Слои: Обзор/Минимум в той же полосе режимов. Плакаты в файле свёрнуты.
- **Дальше:** мёртвый CSS `rp-chat` / `GmCommandCard` не трогал.

### 2026-08-18 — Player `/view` fix wave A–E
- **Статус:** в дереве, без коммита. Тик/Save не гонял. Vite :5173 и API :4173 перезапущены под PIN-strip.
- **A auth:** token в `sessionStorage`; PIN не в JSON при токене; `/api/factions` без уничтоженных и без паролей; catch-all `*` → `/view`; свой ряд державы в payload без `password`/`passwordHash`/`gmNotes` (`maskFactionForIntel` в `filterWorldForFaction`).
- **B–E:** «ОД свободно remaining/cap»; Alt+цифры на рынке/науке; fog fail-closed; path strip → наука; alchemy/stub не в оффере; stance RU; FX keys; фильтры F4–F8/F10.
- **Доказ:** `tsc --noEmit` 0; `test:player-auth` 13/13; OD-тест; `test:viewer-auth` 6; live Belator payload PIN=0, `apMax` 9/`reservedAp` 0; `/vieww` → `/view`.
- **Не в этой волне:** master token в GM-клиенте; `?p=` preview; Galivan в списке входа (есть PIN); nested button/inert; 6 breakthroughs; combat tags.
- **Дальше:** коммит по просьбе. Живой HUD-логин в браузере не кликал (PIN в чат не тащил).

### 2026-08-18 — GM QA: 3-й проход (хвост закрыт)
- **Статус:** landed в дереве, без коммита. Тик/restore не гонял.
- **RP N+1:** `GET /api/rp` отдаёт `episode.rail` (last/preview/prompts/marks без body). `RpGmDesk.refreshRail` больше не фетчит messages на каждый HQ. Live: 36 HQ rails; после открытия стола — 1× `/api/rp` + messages только активного канала. Тест `server/rpRail.test.mjs` 2/2.
- **Map-first:** `rp`/`simulator` в localStorage больше не поднимают студию на reload — карта и F1–F9. Live: stored=`rp` → режим «Карта».
- **Chrome:** ErrorBoundary — кнопка «Перезагрузить». Compact Localhost убран из шапки (превью осталось в попапе шары). Live: 0 кнопки Localhost в топбаре.
- **Дальше:** коммит по просьбе. Auto-restart туннеля с `force:true` не трогал.

### 2026-08-18 — RP стол: шёпот + навигация ГМа
- **Статус:** в дереве, без коммита. `rpSpeakAs.test.ts` 9/9. Browser: HQ Белатор без ложных «шёпот»; шапка «штаб · видит только эта держава»; рельс «Сцены».
- **Фикс:** aside относительно канала (HQ `gm_player` ≠ шёпот); ответ-шёпот целится в автора; в сцене без цели — записка стола; архив не мутирует с меню; пин с confirm; промпт-удаление предупреждает; приказ снимает шёпот.
- **Навигация:** «Сцены» в рельсе + текущая сцена сверху; `]`/`[` и «След. ждут»; клик по цитате прыгает к исходной; «↓ к последним»; выбор на карточке промпта за игрока. Esc плавающего стола не бьёт меню/диалог.
- **Дальше:** досье NPC с ПКМ; правка на стороне игрока.


### 2026-08-18 — Аудит данных, проход 4 (туман / космообъекты / раса державы)
- **Космо-теги vs каталог эффектов:** на 577 системах в `spaceObjects` смесь. 346 тегов есть в `space_objects.json` (астероид, пираты, туманность…) — тик их читает. **509** — только метки POI (`refugees`×356, outpost, beacon, depot…) — экономика молчит. **36× `star`** нет ни в POI, ни в космо-каталоге.
- **Державы без расы:** у Белатора/Турон/Федерации/Роя и др. `primaryRaceId` = null. Расовые замки науки, мнение и беженцы не от кого плясать. `race_states.json` пустой (`races: {}`).
- **Интел** знает те же 15 рас, которых нет в `races.json`. FX без привязки у 25 держав (мажоры привязаны). Туман нарисован только Карнеду и Белатору — для остальных маска пустая (видение от владения/флота), не дыра.
- **Журнал:** в `table-meta` старые `cron_fail: ensureAllFactions is not defined` (потом catch-up ok). GM-интервенция с пометкой ход 59 при столе 17.

### 2026-08-18 — Аудит данных, проход 3 (казна науки / пустые системы / заявки)
- **P1 живой стол:** у Карнеда/Белатора/Амальфеи в `unlockedTechs` лежат **243+94+92 catalogPending** заготовки. Тик **не** скипает их: `collectTechModifierEffects` жрёт `production_mult` (кап канала 2.0), `recomputeUnlocksFromTechs` поднимает `techTiers` (Белатор A9/B10 — от заглушек, не от живого дерева). В очередь игроку их не дают, но бонусы уже в экономике. Ещё Белатор «открыл» несуществующие `tech.path.structural_breakthrough` / `energy_breakthrough`.
- **107 систем без планет** (24 с владельцем): часть — беженские узлы со станциями; часть COR-* пустые владения.
- **Заявки** в `player-orders`/`intents` с ходов 19–66 при столе **ход 17** (тест-хвост). Типы produce_ship/rename/fill_slot без записи в `intents.json` (AP=0) — подтверждено.
- **Не баг:** дубли id нет; combo-техи живут в `tech_combos` (резолвятся); `q_bel_*` — живые сюжетные id не из yearly-каталога.

### 2026-08-18 — Аудит данных, проход 2 (квесты/расы/интенты/граф науки)
- **Новое, не повтор прошлого:** 15 расовых id на планетах нет в `races.json` (252 доли: corinfad, heshah, sikuri, elanor, vendir, lithoid, korvun, taala…). У держав `raceId` живой. Хиноварь несёт свойство `lithogenesis` вне схемы. 35 `catalogPending` техов открывают фейк `catalog.*_scan`. 25 каталожных A-техов требуют более позднюю эру (`asteroid_mining` / `anomaly_tapping`). В `intents.json` нет `produce_ship` / `produce_unit` / `rename_*` / `fill_slot` — в коде пишутся, AP с клиента = 0. `unit.armor_cadre` primary role `vehicle` нет в combat_matchups (нейтральный ×1).
- **Уточнение флота:** `shipDef` ищет и по имени, но регистр важен: живые `разведчик`/`пси-крейсер` не совпадают с «Разведчик»/«Пси-крейсер». `colonyType: none` — пустые миры, не баг; `capital`×2 нормализуется в `core`.
- **Дальше:** не чинил (гонка по модулям + это охота).

### 2026-08-18 — Аудит игровых данных (контент + живой стол ход 17)
- **Статус:** охота, без правок (параллельный агент уже режет UI «модуль как ресурс»). Коммит не делал.
- **P0 корень:** `contentLoader` вливает 20 `modules.json` в `map_resources`; `/api/content` отдаёт смесь (106 = 86 руд + 20 модулей), отдельного ключа `modules` нет. Outfit живёт за счёт этой склейки. UI-фильтры (`isMapDeposit`) уже в EmpireResourceStrip / MarketPanel / TradeColumn / resourcePoolNames; дыры: `GET /api/economy/resource-index`, `getResourcePoolLabels()`, GM ledger все stocks, серверный `buildResourceIndex`.
- **Живой стол:** 978 систем / 35 держав. `logistics.depot` ×35 — id нет в `buildings.json`. 321 здание без `buildingId` (kind+zone fallback). 31/131 групп флота без каталожного корпуса (нет `ship.transport`, дырявые aliases). 130/131 `filledSlots` пустые. `unit.assault` ×2 нет в `units.json`. NPC 500k с жильём, добыча закрыта (Korvun 38/38 gated). Белатор laborGap 98, 51 зданий на pop=1000.
- **Контент:** 6/8 tech_paths без breakthrough; structural/energy cluster ссылает несуществующие doctrine; 387/608 `catalogPending`; 4 мёртвых recipe ingredients; `lint:balance` 0 err / 27 warn.
- **Дальше:** не чинил склейку (гонка с другим агентом). Чинить: развести `modules` vs `map_resources` в public content + дыры aliases/depot/transport.

### 2026-08-18 — Наука: начал реализацию по `SCIENCE_ORBIT_REDESIGN_SPEC.md`, шаг 1 (данные) уже закрыт другим агентом
- **Находка:** параллельный агент уже дотегировал все 430 нетегированных технологий явным полем `direction` (рабочее дерево, без коммита, `content/core/technologies.json` +430) И добавил WARN-валидатор в `scripts/validateTechnologies.mjs` (строки ~258-290) — ровно то, что просила спека §3.1. `npm run validate:tech`: `INFO 0 technology(ies) resolve direction via industry fallback only`.
- **Проверил качество тегирования** (не поверил на слово): не рандом/дефолт — согласовано с реальными правилами `tech_directions.json` (industry получает пути structural/energy/biological/exotic — 4 из 8 RoleScore-путей по дизайну самой схемы, не по лени агента). Пересчитал резолвинг напрямую: industry 355, culture 123, military 69, commerce 60, governance 1, diplomacy 0 (сумма 608).
- **Честная находка, не баг:** дипломатия и управление почти пустые (0 и 1 технология) — это реальный факт по контенту (структурная особенность `tech_directions.json`: 4 из 8 путей завязаны на industry), не ошибка тегирования. §3.2 (защитный потолок угла сектора) один это не вылечит — сектор с 1 технологией не станет визуально весомым при любом угле. Фаза 0/1 из §1d (пустой сектор = просто дверь, без кластера) как раз рассчитана на такой случай — это не противоречие, а подтверждение, что дизайн фаз был не зря.
- **Осторожно:** `ResearchPanel.tsx` сейчас правится и другим параллельным агентом (см. запись выше про P2 бейдж науки/`catalogPending`) — при пересборке экрана свериться, что этот фикс не потерян.
- **Ещё находка:** шаг 2 (`computeOrbitLayout`) и часть шага 5 (`silhouetteShapes.ts`, координаты силуэтов заново выведены из `OrbitSilhouette.dc.html` — мой `genProgression.mjs` не нашли, он был скретч-файлом сессии, не в репо) — тоже уже сделаны другим агентом, некоммичено, `src/viewer/research/orbit/`. Прочитал `computeOrbitLayout.ts` целиком, прогнал тесты реально (не поверил на слово) — **6/6 зелёных**, код корректный: d3-hierarchy `partition()` для угла, честный потолок 135° с перераспределением (§3.2), кольца растут по дуговой длине, позиции стабильны при доливке контента (тест на побайтовую идентичность первых колец).
- **Мой вклад в этот заход:** дописал `computeDirectionProgress.ts` (фазы §1d, 10/10 тестов) и `computeHorizon.ts` (горизонт-клиппинг §1c, 7/7 тестов) — оба чистые, без React. Важное упрощение по ходу: фазы 1-3 (Пробуждается/Растёт/Освоено) — это ОДИН И ТОТ ЖЕ алгоритм (фронтир ≤8, остаток ≤14 напрямую/бейдж), не три режима отрисовки; отдельно кодировать нужно только Фазу 0 (дверь) и Фазу 4 (трофей).
- **Итог по чистой логике:** 3 модуля, 23/23 теста зелёных, всё в `src/viewer/research/orbit/`. Слой готов к вызову из компонента.
- **Дальше:** шаг 3 — сам React-компонент экрана (SVG-рендер секторов/узлов, always-visible верх с очередью+офферами, боковая полоса специализации) — ещё не начат никем. Это самый большой оставшийся кусок.

### 2026-08-18 — Player `/view` hunt ≥100 (Белатор, ход 17)
- **Статус:** каталог 100+ в чате. Тик/Save/коммит не делал. Секреты в чат не писал.
- **P2 починен ранее:** бейдж науки 121→42 (`catalogPending`).
- **P0:** дефолтный мастер-токен в бандле + `*` → GM `/` + PIN в query/body.
- **P1:** «ОД 0/9» = занято не остаток; Alt+1–4 биржи/науки мертвы; HQ stance при Удержании.
- **Дальше:** чинить по просьбе (с P0/P1).

### 2026-08-18 — Экономика: добыча T1–T3 + пег + UI rate; скиллы
- **Статус:** landed, **без коммита**. `depositExtract`+labor **44/44**. `tsc --noEmit` чисто. `lint:balance` 0 err / 27 warn (specialty metal чуть выше кривой). Live audit: **PEG EXTRACTION ZERO = []**.
- **Движок:** потолок добычи `max(freeBuildTier=3, techTiers[cat])`. Свой `treasuryPeg` обходит потолок (шахта всё равно нужна). UI `productionData` больше не плюсует тир плитки без гейта.
- **Скиллы в `.cursor/skills/`:** `gmap-economy-canon`, `systematic-debugging`, `verifying-in-browser`, `accessibility-auditing`, `pixijs-rendering`, `card-game`, `dialogue-systems`, `vercel-react-best-practices`, `webapp-testing`.
- **Живой стол ещё кривой:** NPC 500k без шахт (только жильё) → ambient D/E, 0 A/B; Белатор 51 зданий на pop=1000 (1 labor vs need 3–6). Не трогал published.
- **Дальше:** форпосты 1000 голов + пачки зданий; здания-specialty в полосу metal; коммит по просьбе.

### 2026-08-18 — GM QA: серверный хвост (без коммита)
- **Сделано:** dry-run restore больше не врёт `ok`; spawn-catalog не бампит rev второй раз; каталог не выходит из `content/` (`../`); POST `/api/gm/cockpit/backups/restore` + кнопка в брифе F9; Ops paint подтягивает `world` в store.
- **Проверка:** eslint затронутых `.mjs` чисто; `node --test` catalog/dryRunRestore/backupPath **6/6**. Live `processTurn` / restore снимка не гонял.
- **Дальше:** коммит по просьбе.

### 2026-08-18 — GM QA: 2-й проход (скиллы + фикс RP/Esc/chrome)
- **Скиллы:** `gmap-bug-hunt` → `systematic-debugging` → Playwright 5173. Живой стол: ход 17, кампания на месте; GET `/api/players/share` 200; консоль 0 error.
- **Быстрый пакет держится:** F5 = «Дипло · бой» без оверлея; ⇧F5 = слой Политика; держава Белатор в шапке и Intel; Intel «Юнит» 12 позиций; Тик открывается/закрывается без хода.
- **Починено в этом проходе (без коммита):** два RP-стола (float гасится в режиме RP Стол); сцена «Служебный» не прыгает на HQ; Esc в textarea не закрывает float; сирота `f1` скрыта; TopBar z=400 / tick backdrop 500 — «Тик» кликается поверх workbench.
- **Серверный хвост:** закрыт блоком выше.
- **Дальше:** коммит по просьбе.

### 2026-08-18 — Флот: `FLEET_OUTFIT_RESOURCE_SPEC.md` прочитан целиком, первая сводка была неточной (исправлено)
- **ИСПРАВЛЕНИЕ:** более ранняя запись в этом файле утверждала "~150-200 позиций расписано в спеке" — это неверно, прочитано было по битой перекодировке черновика, не по самому файлу. Реальный файл (`GMap/docs/FLEET_OUTFIT_RESOURCE_SPEC.md`, 488 строк) прочитан целиком с диска. Владелец: "ты очень сильно упростил суть документа".
- **Что документ реально задаёт:**
  - **10 "полок" обвеса** (§4): кинетика, луч/энергия, ракеты/торпеды, авиация, броня, щит, ПКО, двигатель/ФТЛ, сенсор, утилита. У нескольких (авиация, ПКО, полноценная броня) сейчас нет ни слота, ни модуля — только боевая роль или пустой слот.
  - **Недостающая боевая МЕХАНИКА, не только контент** (§2.3, §5, §12): треугольник кинетика→щиты, луч→броня, ракеты/авиация обходят слои но ловятся ПКО — сейчас `combatResolve/properties.mjs` теги kinetic/laser/missile вообще не читает, слот брони в матчапе не участвует. Это код-гэп, не только контент-гэп.
  - **Целевой объём первого прохода — скромный**: §3 и §12 явно называют **~12 стволов + ~9 защит** (семья+тир, без уникальных id на каждое лорное имя). Большие списки имён в §13 — это лор/калибровка тиров, документ прямо пишет "в GMap они не становятся отдельными id" — НЕ список контента для авторства.
  - Правило стоимости база+именная надбавка (T1 bulk, T2+ конкретный именной ресурс) — было верно в первой сводке.
  - §14: 6 открытых вопросов не решены (ПКО — слот или свойство; авиация — `hangar` или тег `craft` на weapon; читает ли карточный бой те же теги, что автобой; и т.д.). §15: порядок внедрения на 6 шагов, где шаг 2 — код (`propertyCombatBreakdown`), не контент.
- **Правильная рамка:** это не столько "конвейер контент-авторства по образцу tech-path-catalog", сколько смешанная задача код+контент с конкретным умеренным объёмом. Конвейера для флота пока нет (в `docs/agents-content/` только `PATH_SIGNATURE_CATALOG`), но не факт, что он вообще нужен при таком малом целевом объёме — решать вместе с владельцем.
- **Дальше:** не начато. Обсудить с владельцем: с чего начинать — §14 открытые вопросы, или сразу §15 шаг 1-2 (зафиксировать полки + научить бой читать теги)?

### 2026-08-18 — GM frontend: наложения / дубли chrome
- **Статус:** 4-й проход — RP стол fill. Без коммита. `tsc --noEmit` чисто. 1600×900: стол 1600×761, колонки 280 / 960 / 360, `rightGap=0`, compose не обрезан, имена не режутся.
- **Причина дыры справа:** `.rp-gm` grid во flex без ширины + колонки max 180/210; шелл `100vh-48px` при шапке 93px.
- **Фикс:** `.rp-gm--fill` на всю ширину; студия без лишней обёртки; шелл `100dvh` + `flex: 1 1 0`; `html,body { margin: 0 }`. Плавающее RP в шапке скрыто, когда уже студия «RP Стол».
- **Проход 3:** пресеты слоёв только на «Слои»; hint инструментов снят; toggles только если панель закрыта; казна свёрнута.
- **Дальше:** строка превью `/view` в шапке; emoji в каталогах.

### 2026-08-18 — Наука: редизайн ЗАФИКСИРОВАН, спека готова — `SCIENCE_ORBIT_REDESIGN_SPEC.md`
- **Финальное решение владельца:** орбита направлений (не колонки/DAG, не web-паутина) — угол сектора = доля реального контента, радиус кольца = вычисляется (без наложений на любом масштабе, проверено на реальных 608 техах), горизонт-клиппинг + кластер-бейджи, 5 фаз на сектор (Спит→Пробуждается→Растёт→Освоено→Завершено, позиции узлов не двигаются между фазами), силуэт-кластеры по мотивам Path of Exile (шестерня/клинок/пламя/самоцвет/кольца/корона), drag-to-combine вместо кнопки "Прорыв" (перетащить 2 изученные технологии направления на флагманский узел). 11 артбордов на дизайн-канвасе: https://claude.ai/code/artifact/1cb5dfb4-9f7f-4fc0-a866-8efe2a1c5d31.
- **Спека:** `agent-tasks/SCIENCE_ORBIT_REDESIGN_SPEC.md` — полная, с техническими рекомендациями (`d3-hierarchy` для математики раскладки вместо ручных формул, `cytoscape.js`/`react-flow` вместо самописного `TechGraphCanvas.tsx` на 608 строк), найденной причиной перекоса Индустрии (только 178/608 техов имеют явное поле `direction`, остальные падают в industry по фолбэку — нужен контент-фикс + защитный потолок угла), явным предупреждением "это пересборка, не патч поверх `ResearchPanel.tsx`/`app.css`", порядком реализации (данные → математика → каркас → фазы → силуэты → комбинация, в таком порядке).
- **Не в скоупе этой спеки:** Лаборатория алхимии (не трогаем), реальное дописание 6 сломанных `tech_paths.json` прорывов (отдельная давно отложенная задача, но её отсутствие блокирует именно drag-to-combine — остальное можно катить без неё), полный каталог технологий, мобильная раскладка.
- **Дальше:** спека готова к реализации — ждёт владельца (делегировать агенту или брать самим). Рекомендация по модели: не Haiku — нужны реальные дизайнерские решения по ходу (силуэты, пороги фаз), как и с RP-чатом.

### 2026-08-17 — независимая верификация двух сданных агентских отчётов (DEPOSIT_ALIASES_SPEC + RP_CHAT_CONSOLIDATION_SPEC)
- **Deposit aliases (коммит `b9f6d73`):** проверено НЕ на слово — независимый скрипт по живому `data/published.json` + `id-aliases.json` + `map_resources.json` даёт **1734 total / 0 unresolved** (совпадает с заявленным). Коммит подтверждён как single-file diff (`content/core/id-aliases.json`, +41/-1). `npm run validate:recipes` и `validate:tech` чисты (0 errors; те же заранее известные WARN про tech_paths/catalogPending, ничего нового).
- **RP-chat consolidation (в рабочем дереве, БЕЗ коммита):** `RpChat.tsx` удалён, нигде по репо (`*.ts/tsx/mjs/js`) не осталось импортов/ссылок (только один безобидный текстовый комментарий в `app.css:5598`, не код). Точечно сверено: `RpStage.tsx:829` — фильтр «Броски» действительно чинен на `m.prompt?.kind==="dice" || (type==="system" && authorName==="Кубик")`; `RpStudio.tsx` теперь рендерит `<RpGmDesk layout="fill">` вместо старого пустых-пропсов пути; `RpGmDesk.tsx` реально импортирует `INTENT_LABELS_MAP`, `ChroniclePanel` (с `canManage`), `ConfirmModal`, поддерживает `layout: "panel"|"fill"`. Прогнал сам: `tsc --noEmit` чисто, `npm run test:viewer-page` **125/125**, `npm run build` — успешно (один пре-существующий по духу bundling-warning: `rpNotify.ts` теперь и статически, и динамически импортируется из-за `RpGmDesk.tsx` — косметика, не баг).
- **Общий health-check после обоих:** `npm run lint` чисто, full server suite **320/320**.
- **Вывод:** оба агентских отчёта — точны, ничего не выдумано. RP-chat правки ещё не закоммичены (ждут владельца, как и остальной рабочий каталог).

### 2026-08-17 — modularization batch: engagements.mjs (FILE_DECOMPOSITION_SPEC batch 16) — все correctness-critical файлы, кроме cardBattle.mjs, готовы
- **Статус:** landed, без коммита. Экспорты те же 25 имён (сверено кодом: missing=[], extra=[], count 25/25). `npm run lint` чисто. `test:combat-engagement` **9/9**. Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/engagements.mjs` 1391 → **39** lines. Вынесено в `server/engagements/`: `store.mjs` (142 — I/O + createEngagement, база без зависимостей), `resolveTick.mjs` (503 — resolveOpenEngagements/advanceEngagement/instantResolveEngagementIds + finishCardEngagement, тоже здесь — иначе цикл с cardActions.mjs), `contacts.mjs` (394 — collectContactsAndAttacks, оставлена ЕДИНОЙ функцией, риск реструктуризации не окупается), `cardActions.mjs` (382 — карточный режим боя, все play/strike/ready/stance/draw/reorder/retreat/trophy действия). Зависимость: contacts → cardActions → resolveTick → store, циклов нет. По пути убрал мёртвый импорт `passCardRound` (был в исходнике, нигде не вызывался). 4 потребителя (`contactStrike.mjs`, `processTurn.mjs`, `routes/engagements.mjs`, `turn/combatIntents.mjs`) не трогал.
- **Файлы:** `server/engagements.mjs`, `server/engagements/store.mjs`, `server/engagements/resolveTick.mjs`, `server/engagements/contacts.mjs`, `server/engagements/cardActions.mjs`.
- **Веха:** это был последний из явно помеченных correctness-critical файлов спеки (engagements/economyTick/combatResolve/normalizeWorld/stabilityRevolt — все 5 готовы). Остался только `cardBattle.mjs` (2932 строки, крупнейший в репо) — по владельцу остановился здесь, дальше по запросу.
- **Дальше:** `cardBattle.mjs` — CCG/боевой движок карт, без выделенного `cardBattle.test.mjs` (косвенно покрыт `cardBattleAuras.test.mjs`/`cardBattleTrophies.test.mjs`), correctness-critical, спека прямо просит идти осторожно. Не начинал.

### 2026-08-17 — modularization batch: economyTick.mjs (FILE_DECOMPOSITION_SPEC batch 15)
- **Статус:** landed, без коммита. Экспорты те же 9 имён (сверено кодом: missing=[], extra=[], count 9/9). `npm run lint` чисто (поймал этим свои же 2 опечатки до прогона тестов — см. Сделано). `test:ambient` 14/14, `test:labor` 25/25. Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Доп. проверка:** `computeFlowBreakdown` — чистая функция (без записи в ledger), прогнал на всех 35 живых державах (`data/published.json`) до/после, канонический JSON **побайтово идентичен** (233,906 = 233,906 байт). `runEconomyTick` мутирует ledger по-настоящему (пишет на диск) — **не гонял его на живых данных** (это было бы реальным применением хода, не тест), вместо этого: максимально механическое построчное копирование (без реструктуризации) + eslint + полный набор тестов + косвенное покрытие (ambient/labor/populationScale/roleScores/spaceObjects test-файлы уже дёргают эту логику).
- **Сделано:** `server/economyTick.mjs` 1318 → **20** lines. Вынесено в `server/economyTick/`: `helpers.mjs` (269 — чистая математика: рост населения/обитаемость, production/upkeep каналы, legacy-мост, налоговые слоты, дефицит), `factionEffects.mjs` (177 — collectFactionEffects, сбор модификаторов для стека), `flowBreakdown.mjs` (236 — computeFlowBreakdown, чистая, потоковая матрица 6 категорий), `market.mjs` (150 — очередь налогов + market/transfer конвертация), `tick.mjs` (535 — сама `runEconomyTick`, оставлена ЕДИНОЙ функцией: там categoryNet/channels/metalDelta/supplyDelta и т.д. протекают через весь цикл по державам, риск реструктуризации не окупается). Односторонняя зависимость: tick → {helpers, factionEffects, flowBreakdown}, циклов нет. По пути сам поймал и исправил 2 свои ошибки при переносе (мёртвый импорт `getEffectiveMarketRates`, придуманное несуществующее имя `flowScaleFor` вместо `logisticsProductionMult`) — eslint поймал бы автоматически, но заметил раньше. 12 потребителей (`api.mjs`, `currencyPeg.mjs`, `planetActions/queries.mjs`, `processTurn.mjs`, `routes/economyMisc.mjs`, `routes/intents.mjs`, `turn/economyIntents.mjs`, 5 test-файлов) не трогал.
- **Файлы:** `server/economyTick.mjs`, `server/economyTick/helpers.mjs`, `server/economyTick/factionEffects.mjs`, `server/economyTick/flowBreakdown.mjs`, `server/economyTick/market.mjs`, `server/economyTick/tick.mjs`.
- **Дальше:** остался только `engagements.mjs` (1391) из critical-списка, затем `cardBattle.mjs` (2932) последним.

### 2026-08-17 — modularization batch: combatResolve.mjs (FILE_DECOMPOSITION_SPEC batch 14)
- **Статус:** landed, без коммита. Экспорты те же 20 имён (сверено кодом: missing=[], extra=[], count 20/20). `npm run lint` чисто. `test:combat-role` **3/3**. Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/combatResolve.mjs` 1211 → **28** lines. Вынесено в `server/combatResolve/`: `properties.mjs` (144 — шип/юнит/ресурс-дефы, property-layer matchup math), `veterancy.mjs` (112 — конфиг/уровни опыта, xp-award, xp-loss при апгрейде юнита), `defenseLayers.mjs` (175 — стационарные защитники из зданий + orbital/surface degrade-счётчики для фазового штурма), `fightMath.mjs` (427 — общее ядро: gatherGroups/rolePower/totalPower/applyCasualties/cleanupEmptyComposition, используется ОБОИМИ resolve-функциями), `resolve.mjs` (393 — resolveEngagementFight + resolveAssaultPhase). Односторонняя зависимость: resolve → fightMath → {properties, veterancy, defenseLayers}, циклов нет. По пути поймал и убрал случайный лишний ре-экспорт `collectPowerMindStrikeMult` через fightMath.mjs (не было в оригинале — сам добавил и сам заметил при ревью до коммита правок). 13 потребителей (`cardBattle.mjs`, `engagements.mjs`, `boarding.mjs`, `stabilityRevolt/revoltMachine.mjs`, `techActions/*`, `diploOffers/*` и др.) не трогал.
- **Файлы:** `server/combatResolve.mjs`, `server/combatResolve/properties.mjs`, `server/combatResolve/veterancy.mjs`, `server/combatResolve/defenseLayers.mjs`, `server/combatResolve/fightMath.mjs`, `server/combatResolve/resolve.mjs`.
- **Дальше:** остались `engagements.mjs` (1391), `economyTick.mjs` (1318) из critical-списка, `cardBattle.mjs` (2932) последним.

### 2026-08-17 — modularization batch: normalizeWorld.mjs (FILE_DECOMPOSITION_SPEC batch 13)
- **Статус:** landed, без коммита. Экспорты те же 4 имени (`loadAliases`, `resolveAlias`, `normalizeWorld`, `normalizeEngagement`). `npm run lint` чисто. Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Доп. проверка (файл без выделенных тестов, спека прямо предупреждает "every field is load-bearing"):** прогнал `normalizeWorld()` на реальном живом борде (`data/published.json`: 978 систем, 67 флотов, 27 легионов, 35 держав, 23 расы, 19 квестов) до и после переноса, канонический (deep key-sorted) JSON — **побайтово идентичен** (3,045,812 байт = 3,045,812 байт). Не просто "тесты прошли" — реальное доказательство нулевого дрейфа полей на реальных данных.
- **Сделано:** `server/normalizeWorld.mjs` 630 → **154** lines (оркестратор + `normalizeEngagement` оставлен как есть — маленький, независимый). Вынесено в `server/normalizeWorld/`: `helpers.mjs` (103 — алиасы/дипло-катализ/normalizeCompGroup/normalizePlayerOrder), `systems.mjs` (58 — чистая normalizeSystems), `forces.mjs` (42 — normalizeFleets/normalizeLegions), `factions.mjs` (322 — самый большой и хрупкий кусок: расы + миграция/бэкафилл договоров + форма NPC posting/council/internalBlocs). Склейка rebel-stub-faction (нуждается в уже нормализованных fleets/legions) осталась в оркестраторе, не в factions.mjs. 6 потребителей (`processTurn.mjs`, `stability.mjs`, `revolt.mjs`, `loyalty.mjs`, `economyTick.mjs`, `courtGovernance` — обратный импорт через syncNpcPassiveEffects) не трогал.
- **Файлы:** `server/normalizeWorld.mjs`, `server/normalizeWorld/helpers.mjs`, `server/normalizeWorld/systems.mjs`, `server/normalizeWorld/forces.mjs`, `server/normalizeWorld/factions.mjs`.
- **Дальше:** остались `engagements.mjs` (1391), `economyTick.mjs` (1318), `combatResolve.mjs` (1211) из critical-списка, `cardBattle.mjs` (2932) последним. Планирую применить тот же приём (живой-борд байт-в-байт diff) к economyTick.mjs, раз он тоже без выделенного unit-теста на общий выход тика.

### 2026-08-17 — modularization batch: stabilityRevolt.mjs (FILE_DECOMPOSITION_SPEC batch 12) — first correctness-critical file
- **Статус:** landed, без коммита. Барель отдаёт ровно те же 38 имён (сверено кодом: missing=[], extra=[], count 38/38). `npm run lint` чисто. `test:revolt` **18/18** (включая regression-тест на сегодняшний `standDown` баг — пережил перенос). `test:stability` 8/8 (один транзиентный флейк на промежуточном прогоне, не повторился — тот же паттерн, что в eslint-записи выше, не регрессия). Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/stabilityRevolt.mjs` 628 → **54** lines. Всего 2 файла (консервативно — это плотно связанный конечный автомат мятежа, резать глубже = риск того же класса багов, что уже ловили сегодня): `meter.mjs` (236 — конфиг/клэмп/band, тик стабильности, production-эффекты, чистые query-хелперы rebelCount/majorityRaceId), `revoltMachine.mjs` (414 — spawnRebelForce/resolveRebelEngagement/resolveSecession/applyPlanetRevolt/applyStabilityRevolt как единый связанный блок, включая `standDown` в исходном виде из сегодняшнего фикса). Зависимость односторонняя: revoltMachine → meter, циклов нет. 8 потребителей (`economyTick.mjs`, `loyalty.mjs`, `normalizeWorld.mjs`, `processTurn.mjs`, `revolt.mjs`, `stability.mjs`, оба test-файла) не трогал.
- **Файлы:** `server/stabilityRevolt.mjs`, `server/stabilityRevolt/meter.mjs`, `server/stabilityRevolt/revoltMachine.mjs`.
- **Дальше:** остались `engagements.mjs` (1391), `economyTick.mjs` (1318), `combatResolve.mjs` (1211), `normalizeWorld.mjs` (630) из critical-списка, `cardBattle.mjs` (2932) последним.

### 2026-08-17 — modularization batch: courtGovernance.mjs (FILE_DECOMPOSITION_SPEC batch 11) — last non-critical file
- **Статус:** landed, без коммита. Барель отдаёт ровно те же 20 имён (missing=[], extra=[]). `npm run lint` чисто (новый чек, добавлен другим потоком с этой сессии). Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/courtGovernance.mjs` 608 → **31** line. Вынесено в `server/courtGovernance/`: `council.mjs` (105 — места совета/портфели + `npcAvailable`, база без зависимостей — специально, чтобы избежать цикла между blocs.mjs и passiveEffects.mjs), `blocs.mjs` (131 — внутренние блоки/влияние-поддержка-угроза), `passiveEffects.mjs` (390 — восстановление faction.activeEffects из трейтов/постов/мест, syncNpcPassiveEffects и потребители). Зависимость односторонняя: passiveEffects → blocs → council, циклов нет. 8 потребителей (`courtConsumers.test.mjs`, `courtProposals.mjs`, `courtRoster.mjs`, `economyTick.mjs`, `loyalty.mjs`, `narrative/courtTasks.mjs`, `normalizeWorld.mjs`, `powerPaths.mjs`) не трогал.
- **Файлы:** `server/courtGovernance.mjs`, `server/courtGovernance/council.mjs`, `server/courtGovernance/blocs.mjs`, `server/courtGovernance/passiveEffects.mjs`.
- **Веха:** это был последний файл из некритичного списка спеки (11 батчей: narrative → playerShare → planetActions → techActions → diploOffers → ledger → intel → questEngine → gmCockpit → orderEngine → courtGovernance). Остались только correctness-critical: `engagements.mjs` (1391), `economyTick.mjs` (1318), `combatResolve.mjs` (1211), `normalizeWorld.mjs` (630), `stabilityRevolt.mjs` (628) — плюс `cardBattle.mjs` (2932, крупнейший) последним.
- **Дальше:** перехожу на critical-файлы с повышенной осторожностью (мелкие консервативные извлечения, тест после каждого) — начну с наименее рискованного из пяти.

### 2026-08-17 — eslint installed (owner-approved) — found 4 more real crashes instantly, all fixed
- **Статус:** installed + configured + clean, without коммит. `npm run lint` (new script) exits 0. Full server suite **320/320** (one transient flake on an interim run, non-reproducing — re-ran clean, not a regression).
- **Owner decision:** approved installing eslint after 3 "referenced before available" crashes were found by hand today. `npm install --save-dev eslint` (v10.8.1, flat config only). `eslint.config.mjs` (new) — deliberately minimal, scoped to `server/**/*.mjs` only (client is TS/React, needs a separate config later if wanted): just `no-undef` + `no-use-before-define` (variables only, not functions — function hoisting is valid JS, not a bug). Not a full style-guide config on purpose, to keep signal high.
- **Immediate payoff — ran it once, got 4 more real bugs in under a second:**
  - `server/turn/combatIntents.mjs`: `pushSystemHistory` (real source: `../planetActions.mjs`), `applyMoveFleet`/`applyMoveLegion` (real source: `./movement.mjs`) — all three called, none imported.
  - `server/turn/researchBuild.mjs` (the same file fixed twice already today for `ensureAllFactions`/`adjustStock`): `setKnowledgeLevel` and `updateIntelFromDiplomacy` (real source: `../intel.mjs`) — also called, also not imported. Missed by this morning's fixes because that fix only chased the specific `ensureAllFactions` crash the boot-tick surfaced; nothing had exercised these two calls yet.
- **Сделано:** added the missing imports in both files, verified signatures against the real exports before wiring (`pushSystemHistory(system, entry)`, `applyMoveFleet/applyMoveLegion(world, intent, journal)`, `setKnowledgeLevel(factionId, entityType, entityId, level, opts)`, `updateIntelFromDiplomacy(world, factionId, targetFactionId, relation, opts)` — all match their call sites exactly). Re-ran `npx eslint server` after: **zero errors, whole tree clean.**
- **Файлы:** `eslint.config.mjs` (new), `package.json` (`devDependencies.eslint`, new `"lint"` script), `server/turn/combatIntents.mjs`, `server/turn/researchBuild.mjs`.
- **Дальше:** this is now the 5th and 6th instance of the exact same bug class found today (7 total counting the 3 found by hand before eslint existed) — all in the same `server/turn/` family of files, all missed by tests because none of these specific branches had test coverage. Client-side (TS/React) eslint config not set up yet — same value proposition, separate config needed (`typescript-eslint`), not done in this pass. Recommend running `npm run lint` as a normal step before considering any `server/` change "done" going forward. Коммит по просьбе.

### 2026-08-17 — modularization batch: orderEngine.mjs (FILE_DECOMPOSITION_SPEC batch 10)
- **Статус:** landed, без коммита. Барель отдаёт ровно те же 21 имя (missing=[], extra=[]). Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/orderEngine.mjs` 618 → **33** lines. Вынесено в `server/orderEngine/`: `time.mjs` (113 — чистая математика ETA: gameHourAtTurn/computeFleetMoveEta/computeLegionMoveEta/resolveBuildHours), `queries.mjs` (83 — activeOrders/reservedApFromOrders/normalizePlayerOrder/formatOrderEtaSummary), `create.mjs` (188 — createOrderFromIntent), `resolve.mjs` (252 — resolveDueOrders + resolveFleetOrder/resolveLegionOrder/resolveBuildOrder/resolveResearchOrder). Заодно убрал мёртвый неиспользуемый импорт `refreshSystemBlockade` (был в исходнике, нигде не вызывался — не поведенческое изменение). 4 потребителя (`api.mjs`, `processTurn.mjs`, `routes/intents.mjs`, `routes/playActions.mjs`) не трогал.
- **Файлы:** `server/orderEngine.mjs`, `server/orderEngine/time.mjs`, `server/orderEngine/queries.mjs`, `server/orderEngine/create.mjs`, `server/orderEngine/resolve.mjs`.
- **Дальше:** из некритичных остался только courtGovernance.mjs, затем correctness-critical (engagements/economyTick/combatResolve/normalizeWorld/stabilityRevolt), cardBattle.mjs последним.

### 2026-08-17 — modularization batch: gmCockpit.mjs (FILE_DECOMPOSITION_SPEC batch 9)
- **Статус:** landed, без коммита. Барель отдаёт ровно те же 9 имён (missing=[], extra=[]). Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/gmCockpit.mjs` 640 → **20** lines. Вынесено в `server/gmCockpit/`: `interventions.mjs` (55 — append-only лог GM-вмешательств), `factionOverview.mjs` (301 — buildFactionComparison/buildSessionBrief/listCockpitBackups, отчётность по снимкам), `dryRun.mjs` (182 — runDryRunTick, пробный тик на scratch-копии), `pegDials.mjs` (111 — дилы мультипликатора пега, использует appendGmIntervention из interventions.mjs). 4 потребителя (`api.mjs`, `gmBalance.mjs`, `powerPaths.mjs`, `routes/gm.mjs`) не трогал.
- **Файлы:** `server/gmCockpit.mjs`, `server/gmCockpit/interventions.mjs`, `server/gmCockpit/factionOverview.mjs`, `server/gmCockpit/dryRun.mjs`, `server/gmCockpit/pegDials.mjs`.
- **Служебное:** live-раздел этого файла подошёл к ~120 строкам — перенёс 3 старых блока (correctness sweep round 1, v0.6.0 GitHub, сироты модулей) в `_archive/status/2026-08-17.md`, ничего не потеряно.
- **Дальше:** остались из некритичных orderEngine/courtGovernance, затем correctness-critical (engagements/economyTick/combatResolve/normalizeWorld/stabilityRevolt), cardBattle.mjs последним.

### 2026-08-17 — modularization batch: questEngine.mjs (FILE_DECOMPOSITION_SPEC batch 8)
- **Статус:** landed, без коммита. Барель отдаёт ровно те же 9 имён (missing=[], extra=[]). Full server suite **320/320** (перепроверено и после round-3 фикса `respond.mjs` другим потоком — конфликта нет, файл цел). `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/questEngine.mjs` 676 → **17** lines. Вынесено в `server/questEngine/`: `queryHelpers.mjs` (143 — факт/мир запросы + matchesFilter/buildFilterContext), `effects.mjs` (179 — стоимость и применение одноразовых эффектов квеста), `resolve.mjs` (362 — rollYearlyQuests/resolveQuestChoice/resolveQuestDice/expireQuests/normalizeQuest). 3 потребителя (`processTurn.mjs`, `questActions.mjs`, `storyQuestSpawn.mjs`) не трогал.
- **Файлы:** `server/questEngine.mjs`, `server/questEngine/queryHelpers.mjs`, `server/questEngine/effects.mjs`, `server/questEngine/resolve.mjs`.
- **Дальше:** остались из некритичных gmCockpit/orderEngine/courtGovernance, затем correctness-critical (engagements/economyTick/combatResolve/normalizeWorld/stabilityRevolt), cardBattle.mjs последним.

### 2026-08-17 — Correctness sweep round 3: respondDiploOffer TDZ crash (3rd instance of this bug class today)
- **Статус:** fixed, без коммита. `respond.mjs` loads clean, full server suite **320/320**.
- **Найдено (parallel FILE_DECOMPOSITION_SPEC agent, diploOffers.mjs batch):** `respondDiploOffer`'s `research_pact` branch used `world` at what's now `respond.mjs:157-158` before its own `const world = readLiveBoard()` declaration later in the same function — a TDZ `ReferenceError` on **every accepted research_pact deal**. Flagged, not fixed, per the decomposition spec's own rule (behavior-preserving only, bug fixes are a separate track — this one).
- **Сделано:** moved the single `readLiveBoard()` read up before the research_pact block (verified `recomputeUnlocksFromTechs` already null-guards `world` internally — `techActions/helpers.mjs:311` `if (factionId && world)` — so reading it unconditionally earlier is safe even with no live board), removed the now-duplicate later declaration.
- **Pattern note:** this is the **3rd** "referenced before available in scope" crash found today (`standDown`, `ensureAllFactions` ×2 files, now this TDZ). All three shipped with 0 test coverage on the exact broken path and all three only surfaced via either live boot verification or a careful human/agent read during file-decomposition — none via static tooling, because there isn't any (no eslint configured). Worth asking the owner whether to install eslint now (`no-undef` + `no-use-before-define` would have caught all 3 mechanically) rather than continuing to find these one at a time.
- **Файлы:** `server/diploOffers/respond.mjs`.
- **Дальше:** ask owner about eslint install (currently blocked — `npx eslint` needs network install, not yet approved). Коммит по просьбе.

### 2026-08-17 — modularization batch: diploOffers.mjs (FILE_DECOMPOSITION_SPEC batch 5) + второй баг найден (не чинил)
- **Статус:** landed, без коммита. Барель отдаёт ровно те же 10 имён (missing=[], extra=[]). Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/diploOffers.mjs` 812 → **20** lines. Вынесено в `server/diploOffers/`: `helpers.mjs` (314 — стор офферов, нормализация/валидация/трансфер айтемов сделки, запись отношений), `createOffer.mjs` (100 — `createDiploOffer`, эскроу), `unilateralStance.mjs` (137 — `applyUnilateralStance`: война/эмбарго/разрыв), `respond.mjs` (295 — `respondDiploOffer`, самая большая функция, accept/reject + перевод активов). 3 потребителя (`api.mjs`, `routes/diplo.mjs`, `turn/economyIntents.mjs`) не трогал.
- **Найден баг №2 (НЕ чинил, только флаг):** `respondDiploOffer` (теперь `server/diploOffers/respond.mjs`) — в ветке `research_pact` (было :674-675) вызывает `recomputeUnlocksFromTechs(..., world)`, но `world` объявлен через `const world = readLiveBoard()` **позже в той же функции** (было :686). Это классический JS TDZ (temporal dead zone) — `ReferenceError: Cannot access 'world' before initialization` на **каждом** принятии сделки с договором `research_pact`. Похоже, имелось в виду `worldPre` (уже прочитан парой строк выше для валидации активов) либо блок research_pact должен идти после чтения `world`. Тестов на `respondDiploOffer`/`research_pact` нет — та же картина, что и с `standDown` и `applyEspionage` сегодня: непокрытый тестами путь с обращением к необъявленной переменной. Сохранил как есть при переносе, оставил явный комментарий в коде на месте бага (не сама правка).
- **Файлы:** `server/diploOffers.mjs`, `server/diploOffers/helpers.mjs`, `server/diploOffers/createOffer.mjs`, `server/diploOffers/unilateralStance.mjs`, `server/diploOffers/respond.mjs`.
- **Дальше:** продолжаю по списку спеки. Похоже, стоит предложить владельцу отдельный проход "grep на использование переменных до объявления" по всему серверу — три однотипных бага за один день (`standDown`, `applyEspionage`, теперь этот) намекают, что это не единичный случай.

### 2026-08-17 — modularization batch: techActions.mjs (FILE_DECOMPOSITION_SPEC batch 4)
- **Статус:** landed, без коммита. Барель отдаёт ровно те же 23 имени (сверено кодом: missing=[], extra=[]). Full server suite **320/320**, `test:tech-offers` 17/17, `test:tech-progress` 14/14. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/techActions.mjs` 986 → **36** lines. Вынесено в `server/techActions/`: `helpers.mjs` (416 — резолв тех/апгрейдов, гейты свойств/рас/трейтов/линейджей, модификатор-стек, unlock-эффекты, canBuildWithTech), `queue.mjs` (303 — очередь исследований: canQueueTech/techAvailability/setResearchQueue/accelerateResearch/researchPathTo), `actions.mjs` (299 — мутирующие действия: researchTech/rerollResearchOffer/upgradeResearchedTechGrade/fillResearchedTechSocket/gmGrantTech/researchUpgrade). Зависимость односторонняя: actions → queue → helpers, циклов нет. 14 потребителей (включая `planetActions/`, `orderEngine.mjs`, `routes/economyTech.mjs` через динамический import) не трогал.
- **Файлы:** `server/techActions.mjs`, `server/techActions/helpers.mjs`, `server/techActions/queue.mjs`, `server/techActions/actions.mjs`.
- **Дальше:** продолжаю по списку спеки — дальше некритичные файлы среднего размера (diploOffers/ledger/intel/questEngine/gmCockpit/orderEngine/courtGovernance), затем correctness-critical (engagements/economyTick/combatResolve/normalizeWorld/stabilityRevolt) с повышенной осторожностью, cardBattle.mjs последним.

### 2026-08-17 — modularization batch: planetActions.mjs (FILE_DECOMPOSITION_SPEC batch 3) + bug found (not fixed, flagging only)
- **Статус:** landed, без коммита. Модуль грузится, экспортирует ровно те же 9 имён (`BUILD_QUEUE_MAX`, `listBuildingVariantsForPlanet`, `pushSystemHistory`, `canPlaceBuilding`, `applyPlanetAction`, `planetCapFromBuildings`, `collectPlanetYields`, `previewBuild`, `setBuildQueue`). Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/planetActions.mjs` 1161 → **20** lines (тонкий барель). Вынесено в `server/planetActions/`: `helpers.mjs` (342 — валидаторы владения/колонизации, разрешение зданий/колоний, AP/afford гейты, стоимость), `applyActions.mjs` (622 — сама `applyPlanetAction`, все 9 веток action: build/demolish/colonize/set_colony_type/rename/fill_slot/staff/staff_transfer/upgrade_grade, как единая функция — не резал на под-обработчики, там общие замыкания persistBoard/gateAp/recordIntent, риск не окупает выигрыш в строках), `queries.mjs` (226 — planetCapFromBuildings/collectPlanetYields/previewBuild/setBuildQueue). 6 потребителей (`api.mjs`, `economyTick.mjs`, `orderEngine.mjs`, `planetGrade.test.mjs`, `routes/economyMisc.mjs`, `routes/playActions.mjs`) не трогал — путь `./planetActions.mjs` тот же.
- **Найден баг (НЕ чинил, по правилу спеки — только флаг):** `previewBuild` (было :1102, теперь `server/planetActions/queries.mjs`) — `const cost = scaledCost(def.cost, buildCostMult(factionId, planet));` ссылается на переменную `planet`, которая **нигде не объявлена** в этой функции (есть только `found.planet`, но `{ system, planet } = found` не деструктурировано). Это `ReferenceError` при каждом успешном вызове — маршрут `POST /api/economy/preview-build` (`routes/economyMisc.mjs:134`) сейчас падает 500 на любой корректный запрос (buildingId существует, планета найдена). Теста на `previewBuild` нет вообще, поэтому баг не ловится прогоном. Сохранил баг как есть при переносе (не мой участок работы — правки багов идёт отдельным потоком в этом же `agent-tasks/`).
- **Файлы:** `server/planetActions.mjs`, `server/planetActions/helpers.mjs`, `server/planetActions/applyActions.mjs`, `server/planetActions/queries.mjs`.
- **Дальше:** продолжаю по списку спеки.

### 2026-08-17 — tech_paths validator gap + MEMORY.md correction (found by parallel audit agent)
- **Статус:** fixed, без коммита. `validate:tech` **0 errors, 8 warnings** (was 0/0 — the 8 are real, previously-invisible gaps, not new breakage). `validate:recipes` unaffected (4/4 same warnings as before).
- **Найдено (background audit agent, Alchemy+TechCatalog track):** 6 of 8 RoleScore path breakthroughs (`tech.path.{structural,energy,mobility,cognitive,biological,exotic}_breakthrough`) don't exist in `technologies.json` — a player who grinds RoleScore to 5000 sees a free-looking "Прорыв" button that always fails server-side (`"Неизвестная технология"`). Root cause: this content was authored twice in 2026-08-16 sessions, both landed "in working tree, no commit," both lost before ever being committed. `validate:tech` never cross-checked `tech_paths.json` against `technologies.json`, so this shipped with a clean 0-error validation run.
- **Сделано:** added a `tech_paths.json` cross-check to `scripts/validateTechnologies.mjs` (WARN-level, not error — the content file's own `meta.note` says clusters intentionally stay empty until a real authoring pass, so this is expected content debt, not a hard failure — but it must stay visible). Corrected `agent-tasks/MEMORY.md`'s false "biological filled 2026-08-16" claim and its overstated `canRaiseUnit` "building gate only" claim (real gap is narrower — see MEMORY.md).
- **Не сделано (намеренно):** did not author the missing 6 breakthroughs + ~24 cluster techs — that's content authoring, not a code fix; the existing `PATH_SIGNATURE_CATALOG` delegated workflow is the right tool for it, flagged in MEMORY.md with an explicit "commit immediately after validate:tech passes" warning to stop the loss pattern repeating. Did not touch the UI side (`ResearchPathsPanel.tsx`/`buildPathStripRows.ts` defaulting missing cost to 0, making the broken button look free) — that's a real, separate small UX fix, listed in the owner's problem-map, not done here.
- **Файлы:** `scripts/validateTechnologies.mjs`, `agent-tasks/MEMORY.md`.
- **Дальше:** re-run `PATH_SIGNATURE_CATALOG` for the 6 missing paths, committing each one once `validate:tech` shows it resolved. Separately: make the "Прорыв" button hide/disable (not show a free-looking 0 cost) when its breakthrough tech doesn't resolve, so this fails safe even if content gaps recur.

Архив: `_archive/status/2026-08-13.md` … `2026-08-17.md` (только что перенёс туда: intel.mjs batch 7, LIVE TABLE turn rollback, ledger.mjs batch 6 — все 2026-08-17).
