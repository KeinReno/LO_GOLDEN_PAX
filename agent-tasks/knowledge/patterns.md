# Agent failure patterns

Писать **класс сбоя агента**, не тикет продукта.

Формат: `дата | симптом | корневая причина | правило/фикс`.

- 2026-08-16 | Раздувание контекста через `after_each_chat.mdc` (писал `tmp/summary-*.json` каждый ход) | Конфликтующее правило требовало создавать JSON на каждый чих чата | Правило удалено, синхронизация только по вехам через `agent-tasks/STATUS.md` (≤120 строк).
- 2026-08-16 | Попытка чтения огромных файлов (api.mjs >140k символов, technologies.json >2MB) целиком | Агент не использовал offset/limit или CBM/Grep перед чтением | Введено строгое правило narrow reads + CBM graph first.

