# STATUS archive — 2026-08-16 earlier session blocks

### 2026-08-16 — Frontend audit: Наука/Биржа/Экономика/Силы (read-only, no fixes)
- **Статус:** аудит завершён (4 параллельных агента), правок не делал. Полные отчёты в чате.
- **Наука — "две схемы":** `ResearchRadialTree.tsx` (legacy, категории A-F) монтируется в `ResearchPanel.tsx:925-936` ТОЛЬКО для вкладки `industry` на десктопе; везде остальном (5/6 направлений + mobile) — `ResearchMatrix.tsx` (новая, `direction`-модель). `ResearchOverview.tsx` — третий радиальный вид (донат по direction) для дефолтного экрана. `AlchemyLab` тоже на старой категорийной модели. Мёртвое: `UpgradesComparison.tsx` (не импортируется), ветка `asRoom!==false` в `ResearchPanel`, `/api/economy/tech-offers/reroll`. `TechStudio.tsx` (GM) хардкодит СВОИ (неверные) названия категорий A-F, разошедшиеся с `economy_schema.json`.
- **Биржа — critical:** `economicTrackClient.ts` шлёт на `/api/currency/*`, сервер слушает `/api/diplo/economic/*` (`diploEconomic.mjs:10-11`) — валютный союз/бартер/договор в дипломатии **всегда 404**, кнопки нерабочие (`DealDesk.tsx:739,745`). Плюс: 6 независимых raw `fetch()` за курсами/книгой заявок (`MarketPanel`, `ContactMarketStrip`, `StockpileSection`) вместо стора → рассинхрон после тика; гонка cancel-offer без await (`MarketPanel.tsx:2164-2167`); формула конвертации задублирована в 3 местах (сервер + `stockpileExpressTrade.ts` осознанно + `MarketPanel.tsx:133-161` неосознанно).
- **Экономика:** два `EconomyPanel.tsx` (viewer vs editors) — функционально РАЗНЫЕ (player UX vs GM ledger), не копипаста, но одинаковое имя = путаница; `editors/EconomyPanel.tsx:155-156` игнорирует уже импортированные `BUILD_METAL/BUILD_SUPPLY.id` и хардкодит строки. Каталог категорий A-F задублирован 3-4 раза (`economy_schema.json`+`economyLabels.ts`+`economyFlowTypes.ts`+CSS). 3 разные функции "имя ресурса по id". `ExpenseDonut` (Overview) не учитывает `treasuryPeg`, а `BudgetSection` — учитывает (расхождение для нестандартной казны).
- **Силы:** `ViewerCombatOverlays`/`ViewerPlayCombatOverlays` — НЕ дубли, адаптер-паттерн (ок). `FleetOrderRing`/`OrderTargetBanner` — оба живые, дополняют друг друга (ок). Проблемы: `UnitCardsStudio.tsx:592-634` — третий hand-rolled рендер боевой карты, не переиспользует `CombatCardMeta`/`CardFace` → визуально может разойтись с реальным боем; copy-paste `unit.generic_line` fallback в `LegionCover.tsx` и `forceReadiness.ts`; мёртвый `@deprecated`: `FORGE_METAL_COST`, `DISBAND_METAL_REFUND` (`constants.ts:133,135`), `upgradeAt()` (`compositionOps.ts:125`); raise/disband всё ещё на старом callback-паттерне, не на `viewerOrderSessionStore.flowData` (в отличие от economy/market/research).

### 2026-08-16 — PATH_SIGNATURE offensive/biological/cognitive/exotic/defensive (T0–T5)
- Кластеры RoleScore заполнены по одному в отдельных заходах: offensive (5 id, reviewer PASS), biological (adaptive_evolution/bioship_growth/hive_coordination/rapid_regen), cognitive (retag f_predictive_models/f_psionic_networks + neural_networks/ai_uplift/deep_computation), exotic (path.exotic_breakthrough + relic_integration/anomaly_harvesting/matter_reweaving/wonder_project), defensive (path.defensive_breakthrough + 4 сигнатуры, combat_role_mult каналы). `validate:tech` чисто на каждом шаге. Не коммичено.

### 2026-08-16 — modularization: api + processTurn + MapCanvas
- **Статус:** MapCanvas input+draw extracted. ViewerPage — blueprint done. `quickcheck` 5/5.
- **Сделано:** `api.mjs` ~871; `processTurn` ~297; `MapCanvas.tsx` ~1297 + `mapCanvas/*`.

### 2026-08-16 — GMap RoleScore 2→8 (B2 extend)
- **Статус:** landed. `npm run test:rolescore` **20/20**.
- **Сделано:** 8 closed ids tick in `eco.roleScores` (bulk+strategic × max(1,tier), never spent). Old structural/energy still grow. HUD PathsStrip + HQ live. Breakthrough gate keys on all 8 via `roleScoreKey` / `role_milestones`. Thresholds first-pass 5000. Unique `economy_balance.roleScore`.

### 2026-08-16 — GMap tech sockets + grading 1→5
- **Статус:** landed. `14/14`. `validate:tech` 0 errors.
- **Сделано:** P0 grades scale `collectTechModifierEffects` `args.mult`/`amount`. P1 sockets: instant fill/swap; empire-wide `swapUpkeepCurrency` in `addUpkeepDemand`. Thin API `POST /api/economy/tech/upgrade-grade` + `.../fill-socket`. ResearchPanel `TechProgressControls`.

### 2026-08-16 — GMap economic track (barter / quote / union)
- **Статус:** landed. `npm run test:economic-track` **13/13**.
- **Сделано:** treaties keyed `(pair, track)` — political + economic coexist. Three instruments: barter, frozen bilateral quote, currency union. Unique routes `POST /api/diplo/economic/{barter,quote,quote-settle,union}` + `/api/gm/economic/*`.

### 2026-08-16 — GMap court / NPC roster (v0.5 onto GMap)
- **Статус:** landed. `npm run test:court-roster` **11/11**.
- **Сделано:** GM live upsert/remove/setRuler (raceId required); player posting/recall + seat/unseat unique routes. GM lock/unlock `seat.at_large`. Consumers live: production_mult, stat_mult, pop_growth_mult, npc_task_speed_mult.
