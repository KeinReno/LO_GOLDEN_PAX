# STATUS — единственная точка синхронизации агентов

Правило: **live ≤ ~120 строк**. Старые блоки → `agent-tasks/_archive/status/`.
Не писать сюда каждый ход чата — только вехи / конец нетривиальной сессии / смена трека.

---

## Live

### 2026-08-17 — Техи привязаны к механике: флот ≠ легион
- **Статус:** landed, **без коммита**. `validate:tech` **608/0**. theater+raise+salvage **43/43**. `test:tech-offers` **17/17**.
- **Сделано:** `modules.json` (20, theater space/ground). 21 зданий (цеха орудий/приводов/щитов, биржа, бонд, базар, академия, театр). Корабельные weapon/engine/shield ≠ легион small_arms/armor/kit. Набор: `requireProperties` (киль/линия). Цех 1 модуль/ход, не в A–F.
- **Гейты:** kinetic/laser/missile/plasma/torpedo/chem_drive/small_arms/armor_kit/field_kit + кили корпусов; коммерция/культура — зал биржи/бонд/базар/академия/театр.
- **Дальше:** коммит по просьбе. Дипломатия/управление не наполнялись.

### 2026-08-17 — Лестницы коммерции / культуры / военного дела = 60
- **Статус:** landed, **без коммита**. Live 12×5: коммерция/культура/военное по 60. Индустрия live 40 + 328 catalogPending.
- **Дальше:** см. блок «Техи привязаны к механике».

### 2026-08-17 — Drag рабочих на слотах планеты
- **Статус:** landed, **без коммита**. `test:labor` 25/25. `tsc --noEmit` чисто после фикса `DISC_R` / `techHintForBuilding`.
- **Сделано:** лоток свободных + пипы на радиале и в списке. Drag лоток↔здание↔здание, Shift×5, тап при выборе, `[`/`]` на карточке инспекта. Первый дроп пинит `assignedLabor`. Пульс лотка, если есть свободные и пустые места.
- **Файлы:** `PlayerPlanetManage.tsx`, `PlanetLaborTray.tsx`, `LaborPips.tsx`, `PlanetRadialSlots.tsx`, `PlanetListSlots.tsx`, `BuildingLaborPanel.tsx`, `planetLabor.ts`, `app.css`.
- **Дальше:** проверить на живом столе в инспекте планеты. Коммит по просьбе.

### 2026-08-17 — Техи коммерции / культуры / военного дела
- **Статус:** landed в `technologies.json`, **без коммита**. `validate:tech` **450/0 err / 0 warn**. `test:tech-offers` **17/17**.
- **Сделано:** 12 живых техов (по 4 на направление). Эпоха 1 без prereq → оффер из 3; эпоха 2 открывается после одной из них. Эффекты из словаря, без `raceLock` и без `researchPath`.
- **Коммерция:** `market_ledger` / `caravan_charter` / `bond_houses` / `exchange_clearing` (`civicPath: trade`).
- **Культура:** `civic_rites` / `public_memory` / `patronage` / `state_pageantry` (`civicPath: culture`).
- **Военное:** `drill_code` / `field_logistics` / `muster_yards` / `combined_arms` (песочница до RoleScore-прорыва).
- **Дальше:** коммит по просьбе. Дипломатия/управление не трогались.

### 2026-08-17 — Наука: стол вокруг графа
- **Статус:** landed, **без коммита**. `tsc --noEmit` чисто.
- **Почему дерево было внизу:** `.research-tree-main` перебивался в `display:block` + граф `height: clamp(55vh)` + шит скроллил страницу. Шит Науки теперь фиксирует высоту 92dvh, граф `flex:1` на оставшуюся площадь.
- **Дубли сняты:** фильтры каталога, баннер «Лучший шаг», второй стек обзор/ветка. Пути — свёрнутый fold. Офферы — узкий dock. Вкладки+поиск в одной полосе.
- **touch-action: none** на `.research-graph`, нодах и чипе знания (варнинг `@use-gesture`).
- **Файлы:** `ResearchPanel.tsx`, `ResearchOffers.tsx`, `TechGraphCanvas.tsx`, `app.css`.
- **Дальше:** живая проверка `/view` → Наука. Лаба не трогалась. Коммит по просьбе.

### 2026-08-17 — Census→labor + живой стол 400–600k + занятости
- **Статус:** landed в дереве и на live-столе (ход 66), **без коммита**. Тесты population/labor/deposit/ambient/raise **76/76**.
- **Движок:** `populationScale.mjs` — 1000 голов = 1 labor; `censusLocked` всегда делит. Ambient/labor/raise через labor units. `OCCUPATION_BY_KIND` + HUD «Занятость». Yield депозита → `def.category`. Казармы едят труд, жильё нет.
- **Live:** `rescaleFactionCensus.mjs --apply` (бэкап) + `ensurePegDeposits.mjs --apply`. Белатор 500k / A137 B90 D445 (не миллионы) / solari 46. Амальфея 500k / blumatid 16.7 / занятости рабочие·учёные·военные. Карнед 400k / blumatid 12.6. Галлиан 500k.
- **Скрипты:** `GMap/scripts/rescaleFactionCensus.mjs`, `ensurePegDeposits.mjs`, `audit-economy-live.mjs`.
- **Осталось:** 366 депозитов без alias (русские имена); Турон/Балсагон peg T4–T5 при tech T1 → extraction 0 до науки; `productionData.ts` эвристика UI. Бесхозные 340 миров с ~8.6e9 голов — `clampUnownedCensus.mjs` (захват иначе даёт 3e5 labor).
- **Дальше:** коммит по просьбе; aliases отдельным заходом. Колода строительства разбита по категориям; инспект здания — рабочие ± (`staff` / `assignedLabor`).

### 2026-08-16 — Пересборка Наука(graph_canvas)→Экономика(declutter Склад)→Квесты(unify) + экономика: диагностика "нет производства"
- **Статус:** 3 крупных пересборки landed в рабочем дереве, **без коммита**. `tsc --noEmit` чисто, `quickcheck` 5/5 после каждой волны. Playwright-проверка вживую (dev-сервер :5173, игрок Империя Белатор).
- **Наука:** узловой граф-канвас вместо старой radial+matrix схемы. Новое: `research/graph/techGraphLayout.ts` (чистая раскладка по эре/лейну), `TechGraphCanvas.tsx` (SVG-рёбра + HTML-чипы, pan/zoom, drag-drop когнитио), `ResearchPathsPanel.tsx` (вынесен "Пути развития" из удалённого `ResearchOverview.tsx`). Удалены `ResearchOverview.tsx`, `ResearchMatrix.tsx`. Очередь/прогноз/баннер свёрнуты в компактный левый рейл в `ResearchPanel.tsx`. Найден и исправлен баг: `.research-map .research-graph` тянул `height:100%` от несжатого родителя → страница скроллилась вместо внутреннего pan/zoom (фикс: `height: clamp(420px,55vh,640px); flex:0 0 auto`).
- **Экономика (Склад):** `StockpileSection.tsx` — сегмент-контрол размера лота + 2 главные кнопки (купить/продать) + overflow-меню (`FloatingPopover`) для резерва/каравана/алерта вместо стены из 9 кнопок на карточку. Поведение onConvert/onReserve/onSetAlert/onSendCaravan не менялось. Найден и исправлен баг: `.eco-stock-card__main` (DragCard shell) — имя ресурса схлопывалось в 0px из-за конфликта специфичности с общим `.drag-card-shell{width:168px}` (фикс: составной селектор `.eco-stock-card__main.drag-card-shell`).
- **Квесты:** единый `QuestDossierView.tsx` (шелл + `QuestChoiceResolveBoard` + `QuestObjectiveList`) теперь общий для `QuestDossier.tsx` (игрок) и `editors/QuestPanel.tsx` (GM) — было 3 независимые реализации с расхождением (`QuestDossier` не проверял `canAffordCosts`, 3 разных набора статус-лейблов). Двор↔Квесты: было поле `giverSystemId` реально хранившее id НПС (баг) → переименовано в `giverNpcId`/`giverNpcName`, резолвится в `adaptQuest.ts` по аналогии с `factionName()`. Добавлен индикатор "Над этим работает: {НПС} · до хода N" (поля `assignedNpcId/Name/EtaTurn`, поиск по `npc.currentTask.linkedQuestId`) в `QuestStage.tsx`/`QuestDossier.tsx`, клик — автовыбор НПС в `CourtPanel` через `courtFocusNpcId` (тот же паттерн deep-link, что у экономики).
- **Экономика — "здания есть, производства нет" (аудит завершён, [1187f3c0](1187f3c0-6255-4648-9503-edec1071d106), без правок):** overflow D/E у Белатора (+68.4М/+57М за ход) на **89%** от 2 планет — Мир Поллукс (63.4М pop, 55.6%) и Домус Алиот (38.1М pop, 33.4%); топ-5 планет = 96.65% населения империи (114М). Слотовый баг (постройки сверх `zoneSlotCap`, до 115/48 на "Домус Солис") и штабели `energia.geo_hydro`/farm (×20-40 на планету) кладёт **`GMap/scripts/applyBelatorEconomy.mjs`** (`setColony`: `surfaceSlots = max(cur, len+2)` вместо реальной проверки грейда; `geoTargets`-цикл пушит N зданий без капа) — тот же паттерн у `applyAmalfeaEconomy.mjs`/`balanceKarnedEconomy.mjs`/`applyKarnedEconomy.mjs`. Nomad-генераторы (`seedKarnedRegion.mjs`, `seedExpSystems.py`, `importNomadMap.py`, `enrichNomadSpace.py`) — **не виноваты** (пустые/дефолтные слоты). Миллионное население Поллукса/Алиота — не то, что писал census-скрипт (там 100-200k + `censusLocked=true`); цифры — рост поверх лока уже на живом столе, лок просто не даёт их сдуть обратно. Адара (55/48 слотов, geo×24) — реальный слотовый баг, но население там нормальное (59k/75 cap) — её "нет производства" это тир-дефицит (Энергия T2 −117), не overflow.
- **Дальше:** не закоммичено, данные/скрипты не трогал. Решение владельца: (a) клэмпнуть население Поллукса/Алиота на живом столе до housing cap, (b) добавить потолок в `ambientFlow.mjs` (защита от будущих такого рода перекосов), (c) почистить дубли построек сверх `zoneSlotCap` (Адара и ещё 20 планет belator/amalfea/karned), (d) поправить `apply*Economy.mjs` скрипты на будущее (не влияет на уже испорченный live-стол).

### 2026-08-16 — Frontend audit+fix волна 2: Дипломатия/Двор, Квесты/RP/HQ, Навигация/HUD, GM core tools, GM Workbench (5+5 параллельных Grok 4.6 агентов)
- **Статус:** landed в рабочем дереве, **без коммита**. Сквозная валидация после всех 5 фикс-агентов: `tsc --noEmit` **чисто (exit 0)**, `quickcheck` **5/5**, `smoke` **6/6** (dev-сервер поднят), `test:court-roster` **11/11**, `test:viewer-auth` **6/6**, `test:viewer-page` **125/125**, `validate:races` ok (18 рас).
- **Дипломатия+Двор:** `ContactMarketStrip` больше не глотает ошибку сети; `GmCourtPanel` переведён на общий `courtMasterFetch`/`courtMasterHeaders` (был ручной `X-Master-Token` в 4 местах) + убран дублирующий polling по `tableRevision`; `onTransfer` (дубль `onGift`) вычищен из `ViewerDiploPanel`; метки отношений в ленте (`worldStore.ts`) переведены через `DIPLOMACY_LABELS`; проверка трона в `CourtPanel.tsx` сведена к единому `isRulerSeat()`; вкладки Двора получили `Alt+1-4`; `window.confirm` в `GmCourtPanel` (removeBloc/removeNpc) заменён на `ConfirmModal`.
- **Квесты/RP/HQ:** CRITICAL — дедуп журнала квеста через `logFingerprint`/`mergeLogs` в `adaptQuest.ts` (сопоставление по ходу+автору+тексту, а не по нестабильному id — убрало дубли сообщений в чате квеста); единый источник `ORDER_TYPE_LABELS` теперь только в `orderEtaLabels.ts` (был расходящийся дубль в `PlayerHqPanels.tsx`); GM-квесты с завершением "по целям" (`objectives` без `arc`) теперь видны игроку; удалён мёртвый `StoryTracker.tsx` (192 строки, не рендерился) и `PlayerForcesPanel` (124 строки, 0 вызовов); `choose` обёрнут в `useCallback` (лишняя пересборка keydown-листенера убрана); `window.confirm` в `editors/QuestPanel.tsx` → `ConfirmModal`.
- **Навигация/карта/HUD:** добавлен CSS-токен `--signal-raid` (использовался с хардкод-фолбэком в 3 местах); закрыта лазейка F9→GM-пресет карты для игрока (`useViewerHotkeys.ts`); кнопка логина блокируется на время запроса (`isLoggingIn` в `useViewerAuth`/`useViewerPlayLogin`), `fetchViewerLogin`/`loadFactions` переведены на `postPlayerJson`/`getPlayerJson`; добавлен индикатор "нет связи" в `ViewerPlayTopbar` после 3 подряд неудачных поллов `/api/map-version`; `TurnStampHud` теперь виден и на мобильном; открытие RP-плавающего окна идёт через `navigateViewerRoom("rp")`.
- **GM core tools:** `confirm()` перед удалением флота/легиона/связи в `MapContextMenu.tsx`/`AltQuickInspector.tsx`/`Inspector.tsx` (раньше — мгновенное удаление без подтверждения, в отличие от системы); loading-состояния в `GmTickDialog`/`GmMarketRatesEditor` (не врут "нет данных" до первого ответа); мастер-токен теперь редактируется только в `Toolbar.tsx` (убран дублирующий инпут из `TopBar.tsx`); эмодзи в GM mode-switch (`TopBar.tsx`) заменены на `lucide-react`; hex-цвета в `IntentsInbox.tsx`/`DesktopHostBadge.tsx` заменены на `--signal-*`/`--surface-*` токены.
- **GM Workbench:** мёртвый `GmCommandCard.tsx` (150 строк, action-at-source карточка фракции/системы) — **подключён**, не удалён: смонтирован в `GmWorkbench.tsx` (оверлей на карте без домена / шапка панели при открытом домене), кнопки зовут `setGmLiveDomain`; мёртвый реэкспорт `GmDomainLauncher` убран из `editors/gm/index.ts`; хардкод `race_human` в `gmSeedColony.ts` заменён на резолв расы с тегом `baseline` из контента (с тем же фолбэком напоследок).
- **НЕ фиксилось (архитектурные развилки, решение за владельцем):** дублирующиеся GM-каталоги контента (Studio vs generic-редактор для одних сущностей); модалка vs виджет для GM-версии дипломатии; `GmLiveStage` vs классический режим воркбенча; полное дублирование RP-чат-клиента между `RpStage.tsx` (игрок) и `editors/RpChat.tsx` (GM) — независимые `fetch`, форматирование времени, отправка сообщений.
- **Дальше:** не закоммичено — ждёт решения владельца.

### 2026-08-16 — Frontend fixes: Наука/Биржа/Экономика/Силы (4 параллельных Grok 4.6 агента)
- **Статус:** landed в рабочем дереве, **без коммита** (по правилу). Сквозная проверка после слияния всех 4: `tsc --noEmit` **чисто (exit 0)**, `quickcheck` **5/5 PASS**. `smoke` не запускался (dev-сервер не поднят, ECONNREFUSED — не регрессия).
- **Наука:** убрана вторая схема (`ResearchRadialTree.tsx` удалён; `industry` теперь через `ResearchMatrix` как все направления). Мёртвый `asRoom` вычищен. `UpgradesComparison` подключён к реальному `onResearchUpgrade`/`/api/economy/research-upgrade` (был мёртвый проп — теперь рабочая кнопка апгрейда техи). `TechStudio.tsx` категории/эры теперь из `ECO_CATEGORY_NAMES`/`RESEARCH_ERAS` (были свои неверные хардкоды). `validate:tech` 438/0, `test:tech-offers` 17/17, `test:tech-progress` 14/14. Не тронуто: `tech.geology` дефолт в `questCatalogShared.ts` (сознательно, риск async в atelier форме).
- **Биржа:** CRITICAL пофикшен — `economicTrackClient.ts` теперь бьёт в реальные `/api/diplo/economic/{union,quote,quote-settle,barter}` вместо несуществующих `/api/currency/*` (валютный союз/договор в дипломатии заработали). `MarketPanel.tsx`: await перед refresh книги при отмене/покупке (гонка убрана), формула конвертации теперь одна (импорт из `stockpileExpressTrade.ts`), курсы через единый `fmtRate` (toFixed(3)). `test:economic-track` 13/13, `test:stockpile-trade` 16/16.
- **Экономика:** `economyFlowTypes.ts` категории теперь производные от `economyLabels.ts` (один источник), `chartData.ts`/`displayLabels.ts` резолвят имена ресурсов через общий `resourceDisplayName()`. `OverviewSection`/`ExpenseDonut` теперь учитывает `treasuryCurrencyId` фракции как `BudgetSection`. `editors/EconomyPanel.tsx` → переименован в `editors/GmLedgerPanel.tsx` (импорты в `Toolbar.tsx`/`GmWorkbench.tsx` обновлены), самопротиворечие с хардкод currency id исправлено. Мусорный `useEffect`/ложная `useMemo`-зависимость вычищены.
- **Силы:** мёртвый `@deprecated` (`FORGE_METAL_COST`, `DISBAND_METAL_REFUND`, `upgradeAt()`) удалён. `unit.generic_line` fallback сведён в `resolveLegionComposition()` (`state/forceReadiness.ts`), `LegionCover.tsx` его переиспользует. `UnitCardsStudio.tsx` GM-предпросмотр карты теперь рендерит настоящий `CombatCardMeta` (та же разметка/иконки/keywords, что в реальном бою) — полный фикс, не частичный. `test:card-battle` 17/17, `test:rolescore` 20/20.
- **Файлы:** `ResearchPanel.tsx`, `ResearchRadialTree.tsx` (удалён), `ViewerResearchRoom.tsx`, `TechStudio.tsx`; `economicTrackClient.ts`, `MarketPanel.tsx`; `economyFlowTypes.ts`, `chartData.ts`, `displayLabels.ts`, `OverviewSection.tsx`, `RpsChain.tsx`, `TaxDragStrip.tsx`, `StockpileSection.tsx`, `GmLedgerPanel.tsx` (переименован из `editors/EconomyPanel.tsx`), `Toolbar.tsx`, `GmWorkbench.tsx`; `forces/constants.ts`, `forces/compositionOps.ts`, `state/forceReadiness.ts`, `forces/LegionCover.tsx`, `editors/studios/UnitCardsStudio.tsx`.
- **Дальше:** не закоммичено — ждёт решения владельца. Осталось по желанию: `app.css` мёртвый класс `.research-panel--radial` не чищен; unify raw-fetch курсов/книги биржи в общий стор (`ContactMarketStrip`/`StockpileSection` всё ещё отдельные fetch) — не входило в скоуп этой сессии.

### 2026-08-16 — ViewerPlaySession flowData in order store
- **Статус:** landed. `test:viewer-page` **125/125**. tsc clean on play session + order store + polls/topbar/rooms/dive/effects. Pre-existing: `cardBattlePick.test.ts` TS2739.
- **Сделано:** `flowData` в `viewerOrderSessionStore`. Poll пишет через getState; logout → `null`. `resetOrderForm` не чистит. Topbar/research/market/economy/dive/effects читают store. Play session селектор только для `buildEconomySystemSignals`. ~619 строк. 0 raw `fetch(`. Cancel AP = `mergeCancelAp`.
- **Файлы:** `viewerOrderSessionStore.ts`, `useViewerLivePolls.ts`, `ViewerPlayTopbar.tsx`, `Viewer*Room.tsx`, `ViewerSystemDive.tsx`, `useViewerPlaySessionEffects.ts`, `ViewerPlaySession.tsx`
- **Дальше:** no commit. Не 80-prop dump. Опционально: AP meters в unit orders, catalogs, `economySystemSignals` из play session, hotkeys setters, payload в React state.

### 2026-08-16 — PATH_SIGNATURE offensive/biological/cognitive/exotic/defensive
- Все 5 кластеров RoleScore заполнены по одному в отдельных заходах, `validate:tech` чисто на каждом. Детали → `_archive/status/2026-08-16.md`. Не коммичено.

### 2026-08-16 — GM Studios Architecture, Battle Simulator, Alt+Click Inspector & Content Validation
- **Статус:** landed. `quickcheck` **5/5**, `validate:tech` **453/453 0 err / 0 warn**, `test:viewer-page` **121/121**, `test:card-battle` **17/17**, `test:rolescore` **20/20**, `test:stockpile-trade` **16/16**, `build` **clean (tsc + vite 0 err)**.
- **Сделано:**
  - **Alt + Click Экспресс-инспектор на карте (AltQuickInspector):** Мгновенная плавающая панель по Alt+Click на системах (владелец, столица, спорная, активность, POI), флотах (фракция, стойка), легионах (статус) и связях.
  - **Ролевой стол (RpStudio):** Полноэкранный рабочий кабинет мастера в TopBar с главами, эпизодами, каналами, масками NPC и дайсами.
  - **Боевой полигон & Симулятор (BattleSimulator):** Флот vs Флот, Легион vs Легион, Осада баз. Интерактивный пошаговый ККИ-бой с аурами/прорывом + Монте-Карло бенчмарк (100 боев со статистикой побед).
  - **Конструктор технологий (TechStudio):** Древо наук по категориям A–F и эрам 1–5, грейдинг I–V, сокеты сырья, эффекты.
  - **Конструктор карт и юнитов (UnitCardsStudio):** Корабли и легионы, статы, ауры/кейворды (`escort`, `support`, `overwhelm`, `siege`, `brace`), живой предпросмотр ККИ-карты.
  - **Конструктор сооружений (BuildingStudio):** Планетарные здания, орбитальные станции, космические объекты, трудоемкость и затраты.
  - **Конструктор рас (RacesStudio):** Расширяемый генератор видов, теги, баланс-бюджет признаков, эффекты.
  - **Редактор государств (PolityStudio) & Пульт правил (GameRulesStudio):** Пресеты темпа (Блиц, Стандарт, Хардкор), лимиты ОД и алхимии.
  - **Единый Studio Навбар:** Верхнее переключение разделов в `TopBar` и `App.tsx` с поддержкой всех режимов `gmShellMode`.
  - **Валидация прорывов:** Добавлены канонические определения технологий прорыва для 5 оставшихся путей (`cognitive`, `structural`, `energy`, `defensive`, `biological`) в `technologies.json`.
- **Файлы:** `src/editors/AltQuickInspector.tsx`, `src/editors/studios/*`, `src/editors/TopBar.tsx`, `src/App.tsx`, `src/styles/app.css`, `server/gmContent.mjs`, `technologies.json`.
- **Дальше:** no commit.

### 2026-08-16 — GM Module & UX: Hierarchical Context Menu, Spotter (Ctrl+K), Power Matrix, Dice Arbiter
- **Статус:** landed. `quickcheck` **5/5**, `test:rolescore` **20/20**, `test:card-battle` **17/17**, `test:stockpile-trade` **16/16**, `test:tech-progress` **14/14**, `build` **clean (tsc + vite 0 err)**.
- **Сделано:**
  - **Иерархическое контекстное меню (MapContextMenu):** Вложенные подменю (Владение & Суверенитет, Силы & Дислокация, POI Объекты, Активность сектора, Приказы флота/легиона); hotkeys & badges.
  - **GM Spotter (Ctrl+K / Cmd+K):** Поисковик-споттер в `GmSpotterModal` по системам, державам, флотам, легионам и GM-командам (!команда, @держава, #система).
  - **Матрица мощи держав (Faction Power Matrix):** В `GmHealthExtras` / `OpsHealthPanel` (Health F9): ранги 🥇🥈🥉, полосы мощи, RoleScore по 8 ролям, Snowball Alert.
  - **Дайс-арбитраж спорных приказов (Contested Orders Arbiter):** В `IntentsInbox`: интерактивный бросок кубиков GM (d100) по конфликтующим приказам одного ранга.
  - **Синхронизация сессии и лог GM:** Кнопка споттера в `GmSessionNotch`, интеграция со снимками ходов и Append-only логом вмешательств GM.
- **Файлы:** `src/editors/MapContextMenu.tsx`, `src/editors/gm/GmSpotterModal.tsx`, `src/editors/gm/GmHealthExtras.tsx`, `src/editors/gm/GmSessionNotch.tsx`, `src/editors/IntentsInbox.tsx`, `src/App.tsx`, `src/styles/app.css`.

### 2026-08-16 — GMap Economy in-place trade + Market self-contained hub + Combat CCG polish
- **Статус:** landed. `test:stockpile-trade` **16/16**, `test:combat-engagement` **9/9**, `test:card-battle` **17/17**, `test:combat-role` **3/3**, `quickcheck` **5/5**.
- **Сделано:** In-place складской обмен, 3-факторная пре-валидация биржи и адаптив <1200px, энерго-голодание карт ⚡−N, ауры соседства на фронте, синхронизация clash cinema, индикаторы прорыва Overwhelm в базу.

### 2026-08-17 — Correctness sweep (Claude, owner-directed) + FILE_DECOMPOSITION_SPEC handoff
- **Статус:** 2 real bugs found+fixed, без коммита. Full server suite (`node --test server/*.test.mjs server/routes/*.test.mjs`) **320/320**, full client TS suite **162/162**.
- **Сделано:**
  1. `stabilityRevolt.mjs` — `standDown(world, planet)` was called (:501, :544) but never defined → `ReferenceError` on any garrison-instant-suppress or stability-recovery tick. Defined it (clears revolt fields + `removeForce` + `pruneEmptyRebelFaction`, both already-existing-but-orphaned helpers — clearly meant to be called from here). Added 2 regression tests to `stabilityRevolt.test.mjs`; the recovery-path one genuinely reproduces the crash pre-fix.
  2. `spaceObjects.test.mjs` — "planet deposits stay infinite" test asserted on `.B[1].rate` for a `map.iron` fixture that's `category: "A"` (matches real `content/core/map_resources.json`) — test typo from today's labor-census batch, not a real regression. Fixed to `.A[1]`.
  - **Investigated, already resolved (no action needed):** the 2026-08-16-audit's Pollux/Aliot slot-overflow + 340-unowned-worlds census bug. Live-checked `data/published.json` (turn 66, current): Домус Алиот 167,223 pop / 39 surface buildings (cap 48), Мир Поллукс 263,953 / 36 (cap 38) — both within cap, both far below the old multi-million blowup. Zero planets galaxy-wide over their surface/orbital slot cap. Zero unowned planets with any population (was ~8.6e9 across 340 worlds). Today's "Census→labor" pass already closed this — the "клэмп Поллукс/Алиот" item in the entry below is stale, can be dropped from Дальше.
- **Файлы:** `server/stabilityRevolt.mjs`, `server/stabilityRevolt.test.mjs`, `server/spaceObjects.test.mjs`.
- **Дальше:** broader correctness sweep continuing (this was a first pass: fix what standard test runs surface, not yet a full static/undefined-reference audit — no linter configured in this repo, `npx eslint` needs an install the owner hasn't approved). `agent-tasks/FILE_DECOMPOSITION_SPEC.md` written and handed off separately (continuation of the api/processTurn/MapCanvas/ViewerPage split onto the remaining 18 server + 44 client files still over ~600 lines) — owner is low on Cursor limits, wants a separate Claude session to pick that up in parallel. Коммит по просьбе.

### 2026-08-17 — v0.6.0 на GitHub
- **Статус:** ветка `0_6`, тег `v0.6.0`, push на `origin`. `GMap/package.json` / Tauri / Cargo = **0.6.0**.
- **В коммите:** накопившийся стол 0.5→0.6 (труд/ценз, граф науки, civic paths, модули флот≠легион, viewer play session, двор/квесты, GM studios).
- **Не в git:** `art/` (~385MB постеры), live `table.sqlite*`, `tick-journal.json`, `.claude/settings.local.json`, секреты.
- **Дальше:** aliases депозитов; клэмп Поллукс/Алиот и потолок ambient; дипломатия/управление как civic-лестницы.

### 2026-08-17 — Сироты модулей + живые законы + гейт станций
- **Статус:** в теге `v0.6.0`. theater+laws **30/30**. `validate:tech` 608/0.
- **Цехи:** ион, сканер, лазер-карабин, ПТ, абляция, гусеница. Сканер → слот тактики (space).
- **Станции:** trade=`market_hall`, military=`kinetic`, science=`archive_hall`, relay=`chem_drive`; mining без гейта.
- **Законы:** `open_markets` +5% supply, `state_religion` +1 лояльность — эффекты из `civic_paths`, не хардкод id.

### 2026-08-17 — Correctness sweep round 2: applyEspionage crash (found by parallel audit agent)
- **Статус:** fixed, без коммита. Import fix verified (module loads clean, all 6 call-site signatures cross-checked against `ledger.mjs`/`opinionTick.mjs` exports). **No automated regression test added** — see Дальше, real risk to do carelessly.
- **Найдено (background audit agent, Intel/Fog+Diplomacy track):** `server/turn/intelIntents.mjs`'s `applyEspionage` called `ensureAllFactions`, `readLedger`, `ensureFactionEco`, `adjustStock`, `writeLedger` (all really from `ledger.mjs`) and `bumpOpinion` (from `opinionTick.mjs`) — **none imported**. Registered live in `appliers.mjs` (`"intent.espionage"`), invoked unguarded in `processTurn.mjs`'s intent loop. `processTurn.mjs` wraps the whole tick in try/catch and rolls back to the `pre_tick` snapshot on any throw — meaning **any player submitting an espionage intent would have silently rolled back that entire turn for every faction on the table**, not just their own action. Same bug class as `standDown` (helper called, never imported) but bigger blast radius.
- **Сделано:** added the missing import block. Verified module loads clean and every call site's args match the real export's signature exactly.
- **Why no test added**: `readLedger`/`writeLedger` hit `data/ledger.json` directly (`tableStore.mjs`'s `LEDGER_PATH` is hardcoded, no test/env override, no DI). A naive test calling the success path for real would mutate a real faction's live cognitio stock as a side effect of running the suite.
- **Файлы:** `server/turn/intelIntents.mjs`.
- **Дальше:** same audit agent found `intelIntents.mjs`/`diploOffers.mjs`/`opinionTick.mjs`/`fogStore.mjs` have **zero test coverage** ("exactly the file with the fatal bug has no test, presumably why it shipped"). Real fix: give `tableStore.mjs` a test-time data-dir override, then backfill tests. Own small workstream. Коммит по просьбе.

### 2026-08-17 — modularization batch: playerShare.mjs (FILE_DECOMPOSITION_SPEC batch 2)
- **Статус:** landed, без коммита. Модуль грузится, экспортирует ровно то же (`configureCloudPubToken`, `configureNgrokAuthtoken`, `getPlayerShareStatus`, `startPlayerShare`, `stopPlayerShare`). Выделенного теста на этот файл нет (ops-тулинг, не игровая логика) — проверено полным прогоном. Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/playerShare.mjs` 1260 → **336** lines (остался тонким оркестратором: определение целевого порта, публичный статус, старт/стоп). Вынесено в `server/playerShare/`: `base.mjs` (383 — общий стейт-синглтон + сетевые/процесс-хелперы), `cloudpub.mjs` (234 — провайдер CloudPub), `ngrok.mjs` (151 — провайдер ngrok), `otherTunnels.mjs` (84 — localtunnel + cloudflared), `watchdog.mjs` (173 — health-watchdog и soft-reconnect). Оба потребителя (`api.mjs`, `routes/playersShare.mjs`) как импортировали 5 функций из `./playerShare.mjs` / `../playerShare.mjs`, так и импортируют — путь не менялся.
- **Файлы:** `server/playerShare.mjs`, `server/playerShare/base.mjs`, `server/playerShare/cloudpub.mjs`, `server/playerShare/ngrok.mjs`, `server/playerShare/otherTunnels.mjs`, `server/playerShare/watchdog.mjs` (все новые, кроме первого).
- **Дальше:** продолжаю по списку спеки, файл за файлом с тестами после каждого.

### 2026-08-17 — modularization batch: narrative.mjs (FILE_DECOMPOSITION_SPEC batch 1)
- **Статус:** landed, без коммита. `quickcheck` 5/5. `test:court-roster` 11/11. Full server suite (`node --test server/*.test.mjs server/routes/*.test.mjs`) **320/320**. `tsc --noEmit` чисто.
- **Сделано:** `server/narrative.mjs` 1512 → **10** lines (thin re-export barrel, `export * from`). Two independent sub-responsibilities extracted with zero cross-imports between them: `server/narrative/poiTimersRefugees.mjs` (502 lines — POI/space-object tags, system timers, consequence presets, refugee spawn/convoy, depot attack range) and `server/narrative/courtTasks.mjs` (1015 lines — NPC court tasks, postings, council seating/portfolios, bloc/race leaders, court event ring buffer). All 9 existing consumers (`api.mjs` via `routes/narrative.mjs`, `courtRoster.mjs`, `economyTick.mjs`, `engagements.mjs`, `intents.mjs`, `logistics.mjs`, `processTurn.mjs`, `stability.mjs`, `courtConsumers.test.mjs`) kept importing from `./narrative.mjs` unchanged — barrel re-exports everything, no call sites touched. Behavior-preserving only, no logic changed.
- **Файлы:** `server/narrative.mjs`, `server/narrative/poiTimersRefugees.mjs` (new), `server/narrative/courtTasks.mjs` (new).
- **Дальше:** `courtTasks.mjs` at 1015 lines is still sizable but cohesive (all NPC-posting/council validation share the same shape) — left as one file per spec's "don't chase a number at the cost of a bad split" guidance; could revisit (postings vs. council-seating vs. tasks) in a later pass if it grows further. Next candidates from the spec's inventory: `engagements.mjs` (1391, correctness-critical — extra caution), `economyTick.mjs` (1318, correctness-critical), `playerShare.mjs` (1260), `combatResolve.mjs` (1211, correctness-critical). `cardBattle.mjs` (2932, largest, correctness-critical) deferred to last given its size and lack of a dedicated test file.

### 2026-08-17 — modularization batch: intel.mjs (FILE_DECOMPOSITION_SPEC batch 7)
- **Статус:** landed, без коммита. Барель отдаёт ровно те же 21 имя (missing=[], extra=[]). Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто. (Прогнано после этого — параллельный поток ниже трогал live `data/`, не код; не пересекается.)
- **Сделано:** `server/intel.mjs` 771 → **36** lines. Вынесено в `server/intel/`: `store.mjs` (233 — I/O + уровни знаний: readIntelStore/ensureFactionIntel/getLevel/setKnowledgeLevel/canEspionage), `discovery.mjs` (395 — bootstrapOwnKnowledge/bumpSystemIntel/applySystemDiscovery/updateIntelFromDiplomacy/processIntelTick), `masking.mjs` (160 — чисто трансформации для payload: publicIntelPayload/approximateStat/maskEntityByLevel/maskFactionForIntel). 8 потребителей (`api.mjs`, `cardBattle.mjs`, `diploOffers/*`, `marketOrders.mjs`, `processTurn.mjs`, `questEngine.mjs`, `routes/fogIntel.mjs`) не трогал.
- **Файлы:** `server/intel.mjs`, `server/intel/store.mjs`, `server/intel/discovery.mjs`, `server/intel/masking.mjs`.
- **Дальше:** продолжаю по списку спеки — questEngine/gmCockpit/orderEngine/courtGovernance остались из некритичных, затем correctness-critical файлы, cardBattle.mjs последним.

### 2026-08-17 — LIVE TABLE: turn renumbered 66→15 + D/E stock corruption rolled back (owner-directed) + second ensureAllFactions crash found+fixed via live boot verification
- **Статус:** applied to live `data/published.json`/`data/ledger.json`, **с бэкапом** (`data/turns/manual_20260817_130904_pre_de_rollback/`). Live-verified end to end: server booted, ran a real catch-up tick, turn genuinely advanced 15→16 with sane D/E growth. This is the highest-severity item this session — touches live game state for 26 factions and a second production-blocking crash.
- **Найдено (background audit agent, Economy/Market track):** turn 65→66's tick ran `applyAmbientDE` on raw census population (pre-labor-scale code) instead of `laborPopulation()`, exploding `currency.energia`/`currency.bios` ×4 to **×73,210** for 26 of ~34 factions (e.g. Белатор D 19,024→83,063,668; a test-nation faction D 2,059→150,738,971). Root cause already fixed live (`economyTick.mjs:544` calls `laborPopulation()` now) — this was corrupted stock left behind by the one bad tick, not an active ongoing bug.
- **Owner decision (asked, not assumed):** narrative game is actually at turn 15 — turns 16-66 were automated cron/test cycles, not real play. Owner: relabel the turn counter to 15, rebalance D/E to the pre-explosion baseline "как ты предлагал" (my proposal: turn-65 pre-tick snapshot values, exact numbers from `data/turns/65_2026-08-16T08-29-09-905Z_pre_tick/ledger.json`). Owner separately flagged Белатор/Карнед/Амальфея need individual follow-up attention, Северный рой needs proper treatment, other factions are NPCs (generic is fine), one faction is explicitly a test nation — **that follow-up balance pass is not done here**, this entry is the mechanical data-integrity fix only.
- **Сделано:**
  1. Backed up `published.json`/`ledger.json`/`tick-journal.json`/`table-meta.json` before touching anything.
  2. `scripts/rollbackDeStockCorruption.mjs` (new, dry-run by default, `--apply` to commit — same pattern as `clampUnownedCensus.mjs`): set `published.json`'s `meta.turn` 66→15, rolled `currency.energia`/`currency.bios` back to turn-65 baseline for the 26 affected factions in `ledger.json`. Verified via direct re-read after apply.
  3. **Live boot-verified the fix and found a second real bug in the process**: booting `server serve.mjs` triggers a boot-catch-up tick (missed daily tick since 2026-08-16). First boot attempt crashed: `ReferenceError: ensureAllFactions is not defined` at `server/turn/researchBuild.mjs:302` (`applyAllResearchQueues`) — same bug class as this morning's `intelIntents.mjs` fix, different file, both `ensureAllFactions` AND `adjustStock` missing from its `../ledger.mjs` import (3 and 2 call sites respectively). `table-meta.json`'s `tickAlerts` log independently confirms this exact bug already caused a real `cron_fail` on 2026-08-16T21:01:30 — it wasn't hypothetical, it had already broken a live tick once before today.
  4. Fixed the import, swept the **entire** `server/` tree (142 .mjs files) for the same pattern across all 6 cross-module `ledger.mjs`/`opinionTick.mjs` functions (`ensureAllFactions`, `readLedger`, `writeLedger`, `ensureFactionEco`, `adjustStock`, `bumpOpinion`, plus `appendLedgerEntry`/`defaultFactionEco`) — **no other real instances found** (only false-positive hits in `server/ledger/core.mjs` itself, which is `ledger.mjs`'s own extracted implementation, not a misuse).
  5. Re-booted: catch-up tick now succeeds (`tickAlerts` shows `catchup_ok`), turn genuinely advanced 15→16 live, D/E growth for the tick is sane (Белатор +317 energia/+140 bios, matches the pre-explosion trend) — confirms both the labor-scale fix and this import fix hold under real conditions, not just unit tests.
- **Файлы:** `data/published.json`, `data/ledger.json` (live data, backed up), `scripts/rollbackDeStockCorruption.mjs` (new), `server/turn/researchBuild.mjs`.
- **Дальше:** owner's flagged follow-up (Белатор/Карнед/Амальфея/Северный рой individual balance pass) — separate, not a data-integrity fix, needs owner's design input on specifics. `tickAlerts`' old `cron_fail`/`catchup_fail` entries are historical, harmless to leave. Full server suite re-confirmed 320/320 after this batch. Коммит по просьбе.

### 2026-08-17 — modularization batch: ledger.mjs (FILE_DECOMPOSITION_SPEC batch 6)
- **Статус:** landed, без коммита. Барель отдаёт ровно те же 15 имён (missing=[], extra=[]). Full server suite **320/320**. `quickcheck` 5/5. `tsc --noEmit` чисто.
- **Сделано:** `server/ledger.mjs` 797 → **24** lines. Вынесено в `server/ledger/`: `core.mjs` (331 — I/O + per-faction eco: readLedger/writeLedger/ensureFactionEco/ensureAllFactions/migrateLedgerForWorld/appendLedgerEntry/adjustStock/setStockReserve), `explain.mjs` (328 — чисто форматирование, `sanitizeEconomyExplain` + приватные хелперы разметки), `publicPayload.mjs` (145 — `getFactionPublicEco`/`publicEconomyPayload`, оба игроко-видимые срезы). Самый широко используемый модуль из пройденных — 42 потребителя по всему серверу, барель не даёт им заметить разницу.
- **Файлы:** `server/ledger.mjs`, `server/ledger/core.mjs`, `server/ledger/explain.mjs`, `server/ledger/publicPayload.mjs`.
- **Дальше:** продолжаю по списку спеки — intel/questEngine/gmCockpit/orderEngine/courtGovernance остались из некритичных, затем correctness-critical (engagements/economyTick/combatResolve/normalizeWorld/stabilityRevolt), cardBattle.mjs последним.

Архив: `_archive/status/2026-08-13.md`, `_archive/status/2026-08-15.md`, `_archive/status/2026-08-16.md`.
