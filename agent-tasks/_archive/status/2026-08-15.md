# STATUS archive — 2026-08-14 … 2026-08-16 session blocks (moved 2026-08-16)

Live snapshot replaced; product is **GMap**. Do not treat these as “still unbuilt in GMap”.

### 2026-08-16 — GMap player token leftover mutations
- **Статус:** landed. `npm run test:player-auth` **11/11**.
- **Сделано:** planet/system/intents/orders/engagements/RP accept `x-player-token` OR password via `authenticatePlayerFaction`. Clients: `boardForce.ts` + ViewerPage mutating fetches send the header. Raise/board/disband/tech-offers/peg untouched.
- **Файлы:** `server/api.mjs`, `src/state/boardForce.ts`, `src/viewer/ViewerPage.tsx`, `server/playerAuth.test.mjs`, `docs/PLAYER_AUTH_TOKEN.md`
- **Дальше:** economy research/alchemy/tax/queues still PIN-body; CardBattle/RP/Market clients still send PIN (compat).

### 2026-08-16 — GMap labor slots vs biosLaborScale
- **Статус:** landed. `npm run test:labor` **14/14**. Deposit gate tests still pass.
- **Сделано:** per-planet job slots (`need = laborSlots || tier`) spend population in placement order; extraction and building yield/convert × staffed fraction. Housing does not consume workers. `biosLaborScale` no longer gates production.
- **Файлы:** `GMap/server/laborAllocation.mjs`, `economyTick.mjs` (thin hook), `flowEngine.mjs` (`laborScaleForDeposit`), `laborAllocation.test.mjs`, `docs/LABOR_SLOTS.md`
- **Дальше:** optional `laborSlots` on buildings.json; belt/station still ungated; B1 named ledger / B3 fx rates still open.

### 2026-08-15 — GMap player token (stop x-faction-id spoof)
- **Статус:** landed. `npm run test:player-auth` **10/10**.
- **Сделано:** hashed session token after PIN login; mutations via helper need `x-player-token` or password; bare `x-faction-id` rejected. Raise/board/disband untouched.
- **Файлы:** `server/playerAuth.mjs` + test, `api.mjs` helper, `src/state/playerAuth.ts`, `docs/PLAYER_AUTH_TOKEN.md`
- **Дальше:** PIN still plaintext on the live board; some inline `body.password` routes not token-aware yet.

### 2026-08-16 — GMap produce hire costs pop
- **Статус:** landed. `npm run test:force-raise` **18/18**.
- **Сделано:** `produce_unit` / `produce_ship` go through `applyForceRaise` (pop + currency + 30% ceiling + gates). Force AP stays an order tax; currency billed once. Mutate still refuses net-new counts. System produce chrome: «стоит население».
- **Файлы:** `systemActions.mjs`, `forceRecruit.mjs`, `forceRecruit.test.mjs`, `SystemCommandPanel.tsx`, `docs/FORCE_RAISE_FROM_PLANET.md`
- **Дальше:** remaining spawns are revolt rebels, colonize settlers, GM/editor paint, lore seed scripts.

### 2026-08-16 — GMap GM peg-multiplier cockpit
- **Статус:** landed. `node scripts/smokeCurrencyPeg.mjs`.
- **Сделано:** GM dial `POST /api/gm/peg-multiplier` + GET list. Clamp [0.8, 1.5] in `currencyPeg`. UI: Баланс → «Курс казны (peg)». Persist via `normalizeWorld`.
- **Файлы:** `currencyPeg.mjs`, `gmCockpit.mjs`, `api.mjs` (route), `normalizeWorld.mjs`, `GmHealthExtras.tsx`, `GmBalancePanel.tsx`, `docs/GM_PEG_MULTIPLIER.md`
- **Дальше:** no viewer redesign.

### 2026-08-16 — GMap colonize first-colony fallback
- **Статус:** landed. `npm run test:colonize-pop` **11/11**.
- **Сделано:** if dest system has no eligible owned source, default to faction highest-pop owned planet anywhere. Fail only with no populated owned world at all. UI lists same-system first, then other owned (name + system).
- **Файлы:** `colonizePop.mjs`, `colonizePop.test.mjs`, `PlayerPlanetManage` source select.
- **Дальше:** no manual race sliders; do not spawn pop from thin air.

### 2026-08-16 — GMap colonize pop transfer (v0.5)
- **Статус:** landed then patched (first-colony fallback above). `npm run test:colonize-pop` **11/11**.
- **Сделано:** colonize deducts `colonizePopulation` from an owned source (picker or same-system highest pop); dest gets that mix (1d6 purity). Fail closed if source unowned/empty/too small. Existing `POST /api/planet/action`.
- **Файлы:** `colonizePop.mjs`, `colonizePop.test.mjs`, `planetActions.mjs` hook, `api.mjs`/`processTurn.mjs` `sourcePlanetId`, `PlayerPlanetManage` source select.

### 2026-08-15 — GMap stability_add → loyalty
- **Статус:** landed. `node --test server/stability.test.mjs` **7/7**.
- **Сделано:** stacked `stability_add` now adds into planet `loyalty` (1 amount = 1 point). Revolt still `loyalty < 20`. No v0.5 3-stage meter.
- **Файлы:** `GMap/server/stability.mjs`, `loyalty.mjs` (one call), `stability.test.mjs`, `docs/STABILITY_ADD_LOYALTY.md`
- **Дальше:** `planet.stability` occupation field still unused; `revolt_risk` outside race/tiers still unused.

### 2026-08-15 — GMap raise-from-planet (v0.5 port)
- **Статус:** API + viewer CTA landed. `tsc --noEmit` clean on UI wiring. `npm run test:force-raise` still the server suite.
- **Сделано:** militia from owned planet = pop + `produceForceCost`; ceiling 30%; barracks/shipyard gates reused; disband returns pop not currency. Viewer: **Набрать** in `PlayerPlanetManage`; **Вернуть на планету** in Forces deck when `homePlanetId`.
- **Файлы:** `forceRecruit.mjs`, `api.mjs` raise/disband-raised, `PlanetRaisePanel.tsx`, `ForceDisbandRaised.tsx`, `forceRaiseClient.ts`, `docs/FORCE_RAISE_FROM_PLANET.md`
- **Дальше:** produce deck now shares raise pop rules (see 2026-08-16 produce hire).

### 2026-08-15 — GMap treasury peg conversion
- **Статус:** conversion live. `node scripts/smokeCurrencyPeg.mjs` **21/21**.
- **Сделано:** `rate = 1 · share^1.5 · (20/global)^0.35 · gm[0.8–1.5]`. Uncapped peg→metal/supply after extraction. Legacy floor untouched. Switch penalty N=3 from 0.5 (`pegChangedTurn`/`lastTreasuryPeg`). GM dial `world.meta.gmPegMultipliers` — later got HTTP route (see GM peg-multiplier).
- **Блокеры:** none this pass. Unions / viewer economy UI / v0.5 client out of scope.
- **Файлы:** `GMap/server/currencyPeg.mjs`, `economyTick.mjs`, `normalizeWorld.mjs`, `content/core/economy_balance.json`, `src/state/types.ts`, `scripts/smokeCurrencyPeg.mjs`
- **Дальше:** unions/economic track; viewer to surface `pegConverted`.

### 2026-08-15 — v0.5 table backend P0+P1 (fog /view, occupy, player auth)
- **Статус:** P0+P1 landed. Suite **456**. Live `scripts/smokeViewerMvp.mjs` PASS (needs `node server/serve.mjs` on 4174).
- **Сделано:** hop-1 fog + GET `/view` (never GM `/state`); same-system engage; occupy after wipe / empty enemy system; ground defense→shields speed→accuracy; tick journal + briefing; player tokens (no `x-faction-id`); tax POST; player peg; `table_revision`. Duplicate `factions.id` is 409. Engage loops up to 12 exchanges so land actually changes hands.
- **Блокеры:** none for a two-player table today. P2 `startPlayerSession` / `players:tunnel` not copied.
- **Файлы:** `Golden_PAX_v0_5/server/domain/intel/fog.mjs`, `campaign/viewerPayload.mjs`, `api/routes/{viewer,factionSelf}.mjs`, `domain/forces/occupation.mjs`, `domain/combat/{groundStats,resolveExchange}.mjs`, `campaign/{playerStore,tickJournalStore,occupy}.mjs`, `scripts/{smokeViewerMvp,smokeHttp}.mjs`
- **Дальше:** P2 player tunnel if needed; do not start P3 cosmetics.

### 2026-08-15 — Audit gaps closed (move_cost / engage / tech catalog)
- **Статус:** code gaps from the 8-spec audit are fixed. Spec-author review still open on 6 specs. Suite **439/439**, `validate:tech` 437/0.
- **Сделано:** `move_cost_mult` → MP spend (`movementCost`, admiral 0.8×3 hops = 2 MP). Engage defaults `systemId` from the force (minefield 0.9× challenger). Catalog nested `upgrades[]` stripped (0 live prereqs; parity drops the key vs GMap). Smoke TT2: bypass billing, socket D→E stock proxy, 5c engage. Integrator reverted a smoke-only mutation that put combat role `armor` on `unit.armor_cadre` (GMap is `vehicle`); fixture `anti_armor` now targets `vehicle`.
- **Блокеры:** Claude review of 6 pending specs. Culture/trait `move_cost_mult` still unwired (route collects court+tech only). Race-weight live skipped (RNG). TT2 smoke not re-run after the vehicle retarget (needs `serve.mjs`).
- **Файлы:** `Golden_PAX_v0_5/server/domain/forces/{movement,engage}.mjs`, `api/routes/forces.mjs`, `content/core/{technologies,units}.json`, `scripts/smokeTechTree2.mjs`
- **Дальше:** spec-author review; optional live `smokeTechTree2.mjs` after vehicle retarget.

### 2026-08-15 — Golden_PAX_v0_5 Galaxy migration
- **Статус:** implemented, pending spec-author review
- **Сделано:** Direct-DB import of `GMap/data/published.json` (978 systems / 2774 planets). Population = `planetCapFromBuildings` only; old pop caps building count via percentile (pop-7 → 1 surface slot). Grade from kept buildings. `depot/surface` aliases to factory. Inert space tags kept. Sectors table added (adjacency had cut it). Live: sample 24 sys / 58ms; full map 40ms + persist 813ms. Suite 432. One reverse-duplicate corridor skipped (`link_exists`).
- **Блокеры:** first-pass building-count heuristic. `isCapital`/lore `notes` not copied (spec). High-pop 0-building worlds stay empty.
- **Файлы:** `Golden_PAX_v0_5/server/domain/planets/galaxyMigration.mjs`, `campaign/{galaxyMigration,sectorStore,planetStore}.mjs`, `db/schema.sql`, `scripts/{migrateGalaxy,smokeGalaxyMigration}.mjs`
- **Дальше:** Claude review per GALAXY_MIGRATION_SPEC "How this gets checked". **Do not take:** galaxy migrate **into** v0.5 as a live GMap task.

### 2026-08-15 — Golden_PAX_v0_5 Stability & revolt
- **Статус:** implemented in v0.5 sandbox, pending spec-author review. **GMap** has `stability_add` → loyalty, not this 3-stage meter (OVERRIDE).
- **Сделано:** Faction-wide stability accumulator (not a GMap port). Court `stability_add` → stack flat + decay −1/turn, clamp 0–100, start 50. Stage 1 `<40` production:* ×0.85; Stage 2 `<25` militia rebels via boarding `syntheticCrewGroup`, stance retreat; Stage 3 after 3 turns → `addFaction`/`seedFactionAccounts` + planet/system transfer. Suite 417. Live `scripts/smokeStabilityRevolt.mjs`.
- **Файлы:** `Golden_PAX_v0_5/server/domain/court/{stability,revolt}.mjs`, `server/campaign/{stabilityStore,revoltTick,turn,seed}.mjs`, `db/schema.sql`, `content/core/economy_balance.json`, `scripts/smokeStabilityRevolt.mjs`

### 2026-08-15 — Golden_PAX_v0_5 System adjacency & movement
- **Статус:** implemented, pending spec-author review
- **Сделано:** GMap port `pathfinding`/`logistics`/instant hop-teleport + NEW engine-range / fuel-MP pool (refill every turn, any location). `x`/`y` display-only. Live `scripts/smokeAdjacencyMovement.mjs`.
- **Файлы:** `Golden_PAX_v0_5/server/domain/systems/{pathfinding,logistics}.mjs`, `domain/forces/movement.mjs`, `campaign/{systemLinksStore,planetStore,forcesStore,turn}.mjs`, `db/schema.sql`, `content/core/rules.json`, `scripts/smokeAdjacencyMovement.mjs`

### 2026-08-15 — Golden_PAX_v0_5 Boarding + kind guard
- **Статус:** implemented in v0.5 sandbox. **GMap also has boarding** (`POST /api/forces/board`) — do not call it unbuilt.
- **Сделано:** `canEngage` rejects legion↔fleet; boarding is its own `POST .../forces/:id/board`. Suite 399. Live `scripts/smokeBoarding.mjs`.
- **Файлы:** `Golden_PAX_v0_5/server/domain/{forces/{engage,recruits,recruitment},combat/boarding}.mjs`, `server/campaign/forcesStore.mjs`, `server/api/{routes,contract}/forces.mjs`, `content/core/economy_balance.json`, `scripts/smokeBoarding.mjs`

### 2026-08-15 — Golden_PAX_v0_5 Currency Peg spec
- **Статус:** implemented in v0.5 sandbox. **GMap also has peg + GM dial** — do not call it unbuilt.
- **Сделано:** dominance rate + GM dial; uncapped peg→metal/supply in flowIncome Pass 5; barter vs frozen quote vs currency_union. Live `scripts/playtestCurrencyPeg.mjs`.
- **Файлы:** `Golden_PAX_v0_5/server/domain/economy/currencyPeg.mjs`, `server/domain/diplomacy/economicRelations.mjs`, `scripts/playtestCurrencyPeg.mjs`

### 2026-08-15 — Golden_PAX_v0_5 Court & NPC roster
- **Статус:** implemented, pending spec-author review
- **Сделано:** GM NPC CRUD, seats/portfolios, postings, tasks, blocs display-only; 4 of 7 effect channels consumed. Suite 400. Live `scripts/smokeCourtRoster.mjs`.
- **Файлы:** `Golden_PAX_v0_5/server/domain/court/*.mjs`, `server/campaign/npcStore.mjs`, `scripts/smokeCourtRoster.mjs`

### 2026-08-14 — Golden_PAX_v0_5 Tech Tree 2.0 (P0–P5)
- **Статус:** implemented, pending Claude review
- **Сделано:** grades, sockets, offer/reroll/bypass, race weights, `requiresTech` + `weapon.*` gate, `combat_role_mult`. Live `scripts/smokeTechTree2.mjs`. **GMap has tech offers** (`techOffers.mjs`) — do not call offers unbuilt.
- **Файлы:** `Golden_PAX_v0_5/server/domain/tech/*.mjs`, `scripts/smokeTechTree2.mjs`

Older done blocks (P0–P4, bootstrap) → `agent-tasks/_archive/status/2026-08-13.md`.
